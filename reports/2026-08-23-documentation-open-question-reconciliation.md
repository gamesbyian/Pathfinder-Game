# Documentation open-question reconciliation (2026-08-23)

> **Status:** concluded-positive
> **Last evidence:** 2026-08-23 — direct review of the current references and research instruments indexed by `docs/README.md`, cross-checked against current topic authorities, ADRs, dated reports, the 2026-08-06 loose-thread audit, and the 2026-08-11 future-work reconciliation
> **Decision:** five live-document stale/open-question statements were reconciled against answers already present elsewhere; the remaining items below are genuinely open, explicitly deferred, or gated on new evidence
> **Remaining gate:** none for this documentation reconciliation; each surviving item retains its own evidence or implementation gate

## Scope and method

Current undated references in `docs/` are the live authority. Dated reports, ADRs, `archive/`, and `history/` were used as evidence but historical questions were not counted as live merely because old wording still exists in a frozen record.

The repository code-search index was unavailable during this pass, so the review used the documentation index and direct file reads rather than treating empty search results as evidence. Current solver authorities, hint docs, security docs, architecture/debt contracts, variant research, typing/accessibility contracts, and research instruments were checked against dated evidence and reconciliation reports.

An item counts below when a live document presents a real empirical question, an untested engineering hypothesis, or an explicitly deferred/blocked decision. Pure checklists, compatibility contracts, and known implementation debt are not automatically converted into “questions.”

## Answers found and live documentation updated

| Area | Stale/open wording found | Answer elsewhere | Reconciliation |
|---|---|---|---|
| Hint tooling | `hint-workbench.md` said `hint-candidate-search.mjs` remained only until parity/migration proved the workbench covered it. | `2026-08-11-future-work-hygiene-reconciliation.md` concluded the standalone CLI remains useful for targeted controlled runs and coexistence is not itself a task. | Updated `docs/hint-workbench.md`: coexistence is intentional; removal reopens only for a concrete maintenance/parity need. |
| Hint report provenance | `reports/README.md` still called `hint-selection.json` unexplained/quarantined pending investigation. | The 2026-08-11 reconciliation traced it to the July 2 read-only hint-selection analyzer used to calibrate the shipped curator. | Updated `reports/README.md`: historical calibration evidence, no open task. |
| Distance guidance | `solver-future-work.md` still called the dead-last legacy-distance retry “the live next step.” | The same document and the opt-in ledger later record `STRATEGY_GOAL_ATTRACTION_LEGACY_DISTANCE_RETRY` as built, +3 with zero regressions, and promoted default-ON on 2026-08-23. | Updated the earlier chronology to point to the completed/promoted result instead of advertising already-finished work. |
| Technique-census reverse oracle | The highest-value question and priority list still said to “explain the 14 reverse-oracle gaps” from scratch. | `2026-08-22-technique-census-reverse-oracle-diagnosis.md` reattributed six to repair-probe and eight to admissible-order; explains `R01936` by seed salt 1; shows the other five repair rows do not reproduce under current-code salt controls; and proves all eight admissible-order historical wins require preceding ladder activity at the exact production commit. | Updated `technique-census-second-order-analysis.md` so the live question is only the remaining mutable-state/minimal-prefix mechanism for the eight admissible-order rows, with the cache-clearing/prefix controls named directly. |
| Architectural speed | `solver-architectural-speed-opportunities.md` still put a beam candidate arena/custom-hash prototype in the suggested execution order even though its own later experiment had answered that form. | `2026-08-23-beam-dedup-numeric-key-arena.md` found the useful gain came from avoiding string construction; native numeric-keyed `Map` matched the custom hash-table microbenchmark while preserving semantics, and the typed-array/custom-hash arena was not pursued. | Updated the section/disposition and suggested order: the arena/hash form is closed absent new profile evidence; scoring and fused move/state kernels are the leading remaining beam hot-path work. |

## Genuine open questions after cross-check

| # | Area | Current question / gate | Cross-check result |
|---:|---|---|---|
| 1 | Solver retention | Can beam score/retention changes preserve known viable lineages at proven extinction boundaries without losing current capability? | Still active in `solver-optimization-current-queue.md` Priority 2. Existing lineage/family evidence nominates held-out K-vs-2K/descriptor tests but does not answer them. |
| 2 | Repair search quality | Can deeper repair editing be made predictably state-conditioned from CP-SAT retreat-feasibility boundaries? | Still active in queue Priority 4. Blind rollout/escape and simple deeper-backtrack forms are closed; the exact feasibility-label expansion remains the gate. |
| 3 | Must-cross guidance | Can live prefix state identify when to target, defer, or make a second must-cross approach well enough to justify scoring changes? | Still active in queue Priority 5. Unconditional attraction is already closed; no later cross-family recurrent predictor was found. |
| 4 | Repair routing | Is there a materially different level-blind selector that deserves a protected/full repair tier, or evidence that useful >5M repair capability is systematically stranded? | Broad `isHighInt`/`portal-heavy` gate widening is closed negative and the 5M late-probe cap was promoted. Only a genuinely narrower/new selector or new stranded-depth evidence reopens the protected-tier question. |
| 5 | Legacy-distance retry depth | Would a larger reserve fraction recover more of the six global-swap gains not recovered by the promoted 0.5-reserve dead-last retry without introducing matched-work regressions? | Explicitly untested in `solver-future-work.md`; no later evidence found. |
| 6 | Admissible-order sequence dependence | Which mutable prepared/search state and minimal preceding ladder prefix enables the eight historical admissible-order reverse-oracle wins? | Broad attribution is now resolved. Leading code-derived discriminator is must-pass/must-cross lower-bound cache clearing after reproducing the unmodified historical full-ladder baseline, followed by progressive prefix priming/state diffing. |
| 7 | Conditional technique value | What is each action's incremental solve probability/cost after specific predecessor failures, rather than its standalone/global solve rate? | Existing census data supports partial conditional rankings but final failure status is low-information; current lifecycle/failure-signature joins remain unfinished. |
| 8 | Budget-tranche residual value | When isolated cap/tranche curves are joined to current production reach and `workSpent`, which deep continuations still deserve shared budget? | Cap curves are implemented and current budget-depth conclusions are documented. The current-lifecycle reach/`workSpent` join is explicitly the next scheduler evidence gate. |
| 9 | Operational similarity | Which named profiles/configs actually rank/explore/retain the same material, and where do near-similar pairs first diverge? | Outcome similarity and source taxonomy are known; the bounded operational-similarity census in `solver-technique-operational-taxonomy.md` remains unrun. |
| 10 | Flag-flip pathology | Why does dedup-near-tie retention help one structural population and hurt another, and can live telemetry distinguish the cases? | Static means nominate size/length/density differences but do not provide a stable predictor. Targeted score/tie/duplicate/frontier/lineage telemetry remains the gate. |
| 11 | Scheduler value ceiling | Can an oracle/static evidence-driven scheduler materially beat the current ladder at a fixed shared work envelope before a larger scheduler framework is justified? | `solver-scheduling-policy.md` explicitly makes oracle-frontier and static-policy value the precondition. No matched-work answer exists yet. |
| 12 | Dense-native core | Which remaining packed-key-indexed structures (`visited`, `edgeUsage`, `gateFlags`, `reachBlockedArr`, etc.) are worth converting to dense level-local indexing? | Dense `staticNeighborKeys` was positive; the remaining structures have different allocation/lifetime profiles. Each still needs profile-backed payoff rather than blanket migration. |
| 13 | Beam state materialization | Can compact snapshots/checkpoint-plus-delta/another state architecture beat the already-optimized parent-pointer replay machine? | Untested in the documented record; tree-order replay itself is already optimized and must not be rediscovered as the same idea. |
| 14 | Scoring kernel | Can a compiled `(level, profile, template)` scorer omit impossible/zero-weight work and materially reduce hot-path cost while preserving decisions or producing a favorable measured behavior change? | Still open and now the leading documented beam CPU-profile opportunity. The earlier negative ablation-gate pre-resolution experiment was materially narrower. |
| 15 | Fused move/state kernel | Can fixed neighbor slots, dense mechanic metadata, direct validity checks, and primitive undo storage avoid candidate-array/`UndoToken` overhead? | Still open. `UndoToken` object pooling was negative, but that does not answer replacing the representation. |
| 16 | Work-meter writes | Can bounded local accumulation/flushes reduce dual work-counter hot writes without changing exact budget/provenance semantics? | Open but low priority and explicitly conditional on profiling showing material self-time. |
| 17 | Variant scaling | How do multiple structural factors jointly change solve cost/capability beyond the existing one-axis exact-length tooling? | `variant-level-research.md` explicitly says broader multi-feature scaling remains open. Family/parent discipline and current-code revalidation rules are already settled. |
| 18 | Hint admissible-slack generalization | Does the large admissible-slack enumeration speedup hold across a representative population without changing reachable hint sets? | Current docs contain strong single/bounded examples, including ~12.5× on `P00105`, but explicitly say broader enumeration validation remains incomplete. |
| 19 | Hint outer-CLI parity | Are `hint-diversification.mjs` and workbench outputs byte-equivalent at the outer-CLI layer where they share `hint-ablation-generator.ts`? | Still explicitly untested. This is a parity question, not a reason to reopen standalone candidate-CLI migration. |
| 20 | Firestore admin cutover | Has production admin authorization completed migration from the temporary legacy email fallback to the `admin: true` custom claim? | No. `security.md` and the 2026-08-11 reconciliation agree it is blocked on the required production/Firebase operation; the documented cutover steps remain current. |
| 21 | Firestore behavioral tests | Should emulator-backed Firestore rules tests be added now? | No current trigger. The docs consistently defer them until the next substantive rules change justifies the emulator/tooling cost. This is intentionally deferred, not forgotten. |

## Reviewed material that is intentionally not an open-question list

`architecture-unification-debt.md` remains a live structural-debt queue. Its rows describe known implementation consolidation work, not unanswered empirical questions; no contradictory completion evidence was found during this pass. Likewise, correctness hardening, mechanic-state contracts, level-blindness, budget determinism, typing, accessibility, CSP, architecture, testing, change recipes, solver ablation/shadow/profile instruments, and investigation-report conventions are current contracts/checklists rather than latent research queues.

Historical reports/snapshots can still contain wording such as “next,” “open,” or “pending.” They were not rewritten unless a current index/reference incorrectly projected that old state forward. Frozen chronology remains evidence; current docs own current status.

## Hygiene rule reinforced by this pass

When a question is answered later in the same document or in a dated report/ledger, update the earlier live navigation sentence as well as appending the result. Otherwise an agent scanning headings/priority tables can restart already-completed work without ever reaching the later reconciliation paragraph.
