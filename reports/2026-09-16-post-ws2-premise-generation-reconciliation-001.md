# Post-WS2 premise-generation reconciliation

**Date:** 2026-09-16  
**Scope:** bounded reconciliation after the Class-4 production promotion and Class-2 economics closeout.  
**Authority:** `docs/solver-optimization-workstreams.md` remains the execution-priority authority; this report does not manufacture a new active gate.

## Current boundary

The post-promotion production refresh is **101/102 C1 + 1,169/1,700 C2**, leaving **531** C2 misses. The refreshed residual atlas splits Classes 1-5 as:

- Class 1: 17
- Class 2: 30
- Class 3: 23
- Class 4: 71
- Class 5: 390

This supersedes the pre-promotion 652-row residual and the old 123-Class-4 / 431-Class-5 frontier for future triage. The Class-4 portal dead-last additive retry is promoted; the Class-2 must-turn late-repair economics form is closed negative; the open-path Class-5 observer stopped on natural contrast starvation.

## Reopen-stack reconciliation

The current queue is genuinely idle: no existing reopen condition directly became true merely because the residual shrank. The historical stack was therefore checked as a source of *premises*, not silently promoted into an execution queue.

### Open-path topological completion signature

**Disposition:** completed/stopped, not live.

The implemented observer was joined to existing exact-labelled evidence and found 28 rows in 26 endpoint/geometry/reference-controlled strata with zero natural LIVE/DEAD contrasts. Repeating the same natural-evidence join is not an earned experiment. Any human-parent intervention requires a separately justified question.

### Unfinished full-pool categorical-state projection

**Disposition:** strongest bounded premise-generation candidate; not yet an active solver treatment.

The archaeology register records a genuinely unfinished August observer chain on four exact A/D `DEAD top-ranked / LIVE alternative` parents (`S00001`, `S00030`, `S00048`, `R00104`). Pairwise scalar/simple-interface explanations failed to recur; the planned next step was to inspect the full ranked extinction pools with a tiny prespecified family of already-maintained categorical/interface-state keys, reporting bucket count, singleton share, guaranteed slots, exact-LIVE survival, a random-reserve control, and a modest width-only control. Tooling had been extended to retain the pools, but no committed decision-bearing result was found.

This differs from reopening the closed August quota/crowding forms: the proposed work is read-only counterfactual projection intended to determine whether a recurring categorical distinction exists at all.

**Necessary reconciliation before execution:** confirm that the four historical parents and retained full-pool evidence remain interpretable under current exact-label semantics and that September Class-5 work has not already answered the same question. If still nonredundant, this is the cheapest historically earned observer to run before inventing a new Class-5 human-parent intervention or another scalar feasibility feature.

A positive result would nominate a specific categorical completion-regime premise for controlled replication. A null/heterogeneous result would close this historical thread without changing runtime behavior.

### Future-intersection / arbitrary-target feasibility

**Disposition:** unresolved but not currently earned.

The archaeology establishes that historical production formulations were dirty tests, not a clean premise falsification. The standing reopen condition is still stricter: current exact labels must nominate a specific realizable-future-crossing/event-feasibility distinction. The post-promotion refresh alone supplies no such nomination. Do not revive blueprint/synthetic-must-cross behavior first.

### Cross-attempt basin overlap

**Disposition:** observer remains conditional.

Historical overlap telemetry had transport defects and later retry work had control-plane defects, so the general observer question remains open. But the current queue does not contain an allocation decision whose interpretation depends on whether two actions/retries reach distinct basins. Rebuilding the observer merely because WS2 is idle would violate the question-first rule.

### Dependency-conditioned repair descent shadow

**Disposition:** conditional, not active.

This becomes useful when current WS6 evidence specifically implicates an early/interior commitment that append-only repair cannot revise. The production refresh did not establish that condition.

### Certified forced-chain traversal census

**Disposition:** speed-phase conditional, not active solve-acquisition work.

Historical specialized traversal is suggestive but bundled with other behavior. The modern census is earned only by a current performance question/hotspot showing material cost inside post-prune one-successor chains. It should not be smuggled into the solve-acquisition queue.

### Partial-order / commuting excursion replay

**Disposition:** conditional, not active.

Current exact/accepted-path evidence has not yet been shown here to nominate redundant obligation ordering strongly enough to justify the swap/replay observer.

## Result

There is **no newly earned production solver experiment** from the post-promotion refresh alone.

The best bounded next research action is narrower:

1. reconcile the unfinished full-pool categorical-state projection against current exact-label semantics and September Class-5 evidence;
2. if it remains nonredundant and the retained pools/tooling are usable, run that observer unchanged/read-only on the four historical extinction parents;
3. use the result only to decide whether a recurring categorical completion-regime premise exists and deserves controlled replication.

Do not treat this nomination as permission for a quota/crowding policy, human-parent generation, or a production selector. Those require evidence from the observer plus the normal controlled-contrast/generalization gates.

## Authority repairs prompted by this reconciliation

The post-promotion queue exposed stale summary text in the live docs:

- `solver-optimization-workstreams.md` still listed the already-completed production refresh and already-stopped open-path gate in its WS2 summary-table row despite the live section saying no gates were earned.
- `solver-future-work.md` still carried the old 431/123 Class-5/Class-4 frontier, presented the completed open-path evaluation as pending, and called must-turn economics live after its negative closeout.

Those are live-authority inconsistencies, not historical evidence changes, and should be corrected to the current 2026-09-16 state.
