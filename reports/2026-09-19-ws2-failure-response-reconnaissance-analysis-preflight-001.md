# WS2 failure-response reconnaissance analysis preflight 001

> **Status:** active
> **Last evidence:** 2026-09-19 — current artifact and maintained-producer audit.
> **Decision:** analyze the first ordinary post-contract compact-response population; do not backfill from pre-contract or selectively dispatched runs.
> **Remaining gate:** a protocol-compatible ordinary producer artifact containing its natural solved controls and misses.
> **Evidence role:** prospective analysis precommitment; no outcomes inspected.

## Population and independence

Use the first maintained ordinary solver producer completed after automatic compact-response publication. Freeze its full natural population before analysis. The independent unit is `parentId`; repeated rows/actions are measurements, not independent samples. Retain solved controls when the producer naturally supplies them. Reject unknown/mixed `protocolHash` or `solverRef` for comparative claims.

Stage A consumes only the standard automatic compact document with:

```bash
npm run research:query-failure-response -- --in=<failure-response/compact.json>
```

Do not infer prevalence from historical `failedStrategies`, pre-contract summaries, method probes selected on known failures, or a partial/red artifact.

## Mechanical routing rule

Route to exactly one lane, in this order, from parent-level summaries:

1. **rejection counterfactual** when a recurrent reached/participated boundary has comparable dose and rejection/retention evidence nominates the same boundary;
2. **first-loss / operational divergence** when protocol-compatible repeated policies show materially distinct response but Stage A cannot localize a rejection boundary;
3. **producer-consumer 2x2** when the same parent population shows a recurrent, explicit producer/consumer pairing with both factors observable;
4. **allocation-specific follow-up** when recurrent participation/dose censoring or starvation, rather than operator response, is the discriminating signal;
5. **none** when no lane has a recurrent parent-level opportunity;
6. **unresolved-needs-compact-diagnostics** when Stage A is complete/comparable but cannot discriminate because prune/flow/progress fields are absent.

Only outcome 6 authorizes a frozen Stage-B sample using the research-only compact diagnostic profile. Do not select first-loss by intuition, and do not let this preflight reopen search-loss Phase 9; WS2 authority must record that selection.

## Acquisition disposition

No eligible committed post-contract ordinary population exists as of this audit. `solver-production-replay-baseline.yml` is the maintained ordinary full-population producer, but dispatching it merely for reconnaissance is broad compute. Wait for its next justified run and consume the published compact artifact. Coding is complete; remaining work is GHA/future evidence, then mechanical reduction.
