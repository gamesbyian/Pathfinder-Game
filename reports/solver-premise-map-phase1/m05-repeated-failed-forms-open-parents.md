# M5 — repeated failed forms beneath open parents

Snapshot: `solver-premise-map-v1-2026-09-17` (`e9601ffb8fa304d5ea91d054a9de6cb1bf8ff28e`)
Method: preregistered M5 only. The typed graph was searched for open semantic parents with several explicitly tested implementation forms that failed or closed without closing the parent. This lens deliberately excludes loose thematic families unless the frozen graph/evidence makes the parent/form relation explicit.

## Qualifying family: P032 joint-feasibility reasoning

P032 is `high-value open`. Two explicit children qualify:

- P033, scalar residual descriptors, is `closed as tested form` and both `TESTED_FORM_OF` and `NARROWS` P032;
- P034, categorical event vocabularies, is `closed as tested form` and both `TESTED_FORM_OF` and `NARROWS` P032.

The repeated pattern is therefore real: two compact summary representations failed to provide enough joint-feasibility signal, while the broader operation remains open.

### Interpretation A — the parent survives and form search should remain semantic

The failed forms share a compression strategy: each attempts to summarize joint constraint interactions into a compact descriptor vocabulary. Their failure does not test relational formulations such as shared-resource contention (P157/P163), separator/interface relations (P014/P074/P093), or order/commutativity structure (P091/P169).

Under this interpretation, the repeated failures are evidence against a *representation family*, not against joint-feasibility reasoning. P137 explicitly guards against treating failure of one representation as closure of a broader premise.

### Interpretation B — the parent is too broad to remain decision-bearing

P032's breadth also creates a falsifiability problem. If every failed representation can be replaced by another semantically different form, the parent risks becoming an indefinitely extensible aspiration rather than a bounded research claim. M5 therefore finds pressure to sharpen P032 into operation-specific subclaims with explicit inputs, outputs, and closure conditions.

This interpretation does not say P032 is false. It says repeated failures can be evidence that the parent needs better decomposition rather than another unconstrained implementation attempt.

Confidence: high that the family qualifies; medium on which interpretation should govern future work. Classification: `ONTOLOGY_ISSUE` / `SCOPE_SPLIT` output.

## Non-qualifying near cases

P081 has P035 as one explicit tested form, not several. P076 has P170 as one explicit tested form. These do not satisfy M5's repeated-form criterion.

Several historical ranking, lexical, repair, or randomization experiments contain multiple implementation attempts in source history, but the frozen graph does not encode several failed forms beneath one still-open semantic parent with enough clarity to pass the preregistered test. Treating them as qualifying would silently broaden M5 from typed repeated-form evidence into thematic archaeology.

## M5 bounded conclusion

Only P032 clearly satisfies the strict frozen-graph criterion. That negative result is useful: the map does not support a broad story that many open parents are surviving arbitrarily many failed children. The one strong case simultaneously supports two live readings, semantic sibling search and parent sharpening. M5 does not choose between them and does not feed either reading into later lenses.
