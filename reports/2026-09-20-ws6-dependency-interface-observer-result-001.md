# WS6 dependency-interface observer result 001

> **Status:** concluded-positive
> **Last evidence:** 2026-09-20 — frozen retained-data observer on R03147's Lane-E exact DEAD/LIVE divergence population, 4 matched comparisons collapsing to 2 independent divergence points.
> **Decision:** the observer's development advancement rule is met. Both independent divergence points show a coherent bounded semantic delta in the same two families, path-resource occupancy and relational must-pass lower bound, while global accounting is equal and both DEAD/LIVE branches pass the current hard-prune gauntlet. Nominate independent-parent replication of this bounded interface family; do not build a repair operator.
> **Remaining gate:** freeze the smallest independent-parent exact matched-pair population that can test the same path-resource/relational-bound interface without changing its fields after outcome inspection. If no retained population supplies that contrast, leave WS6 dormant rather than generate broad new truth solely for this nomination.
> **Evidence role:** development

<!-- research-closeout {"schema":"pathfinder.research-closeout/v1","status":"concluded-positive","lastEvidenceDate":"2026-09-20","decision":"both independent R03147 divergence points show the same bounded semantic families (path-resource occupancy and relational must-pass lower bound) while global accounting matches and current hard prunes pass; nominate independent-parent replication only","remainingGate":"freeze and test the same bounded interface on independent parents using retained exact matched pairs if available; no repair consumer before replication","joins":{"researchQuestion":"WS6-DEPENDENCY-CONDITIONED-REPAIR","premiseRefs":[],"measurementOpportunity":null},"evidenceRole":"development","scope":{"populationIdentity":"R03147 Lane-E multi-pick exact matched pairs whose point of no return equals first DEAD/LIVE divergence","selection":"frozen prospectively by reports/2026-09-20-ws6-dependency-interface-observer-reopen-001.md before observer outcomes","inferenceScope":"one-parent development microscope; two independent divergence points with three additional downstream-continuation corroborations"},"claimRefs":[],"sourceArtifacts":["reports/2026-09-20-ws6-dependency-interface-observer-reopen-001.md","reports/stress/lane-e-multi-pick-bisection-2026-09-17.json","reports/stress/class5-multi-pick-lane-e-input-2026-09-17.json","scripts/stress/ws6-dependency-interface-observer.mjs"],"prospective":{"expectation":"ordinary-looking causal moves may differ mainly through local path-history/resource state rather than named obligation counters","surprise":"both independent divergence points differ in the same two bounded semantic families while their global accounting is identical and neither is caught by existing hard prunes","anomaly":null}} -->

## Frozen population and unit count

The observer reused Lane E's already exact-labelled R03147 multi-pick population. No CP-SAT query, solver search, repair rollout or new sampling ran.

The four matched comparisons collapse to **two independent divergence points**:

1. DEAD `R03147:multi-pick-218` vs LIVE `R03147:multi-pick-262`;
2. DEAD `R03147:multi-pick-975` vs LIVE `R03147:multi-pick-112`, with three distinct downstream DEAD continuations corroborating the same divergence point.

The parent count is **one**. Downstream continuations do not inflate independence.

## Result

Frozen classification:

- comparisons: **4**
- independent divergence points: **2**
- existing-hard-fact: **0 / 2**
- bounded-semantic-delta: **2 / 2**
- geometry/history-only at this resolution: **0 / 2**
- blocked: **0 / 2**
- coherent semantic families across both points:
  - `pathResourceCounts`
  - `relationalBounds`

The preregistered development advancement rule required both independent divergence points to show a coherent bounded semantic delta in the same semantic family, or an existing exact fact that clearly defined a new testable relation. The first condition is met.

## What did not distinguish DEAD from LIVE

At both divergence points, the immediately post-move states agree on the frozen global accounting fields:

- counted length used/remaining;
- intersections used/remaining;
- pending must-pass count;
- pending must-cross count;
- pending must-turn count;
- pending surround count;
- pending adjacent-turn count;
- flipper-used mask;
- portal-jump count / used portal pairs.

The branch targets are also ordinary:

- previously unvisited;
- no prior target edge usage;
- non-portal moves.

And both exact-DEAD and exact-LIVE branches **pass** the current production hard-prune gauntlet. No existing sound prune explains the distinction.

## Divergence point 1

`multi-pick-218` DEAD moves south from [11,6] to [11,7].  
`multi-pick-262` LIVE moves west from [11,6] to [10,6].

Global accounting is identical, including 4 counted steps used, 101 remaining, 0/7 intersections used/remaining, and the same pending-obligation counts.

Local path-resource projection:

| field | DEAD | LIVE |
|---|---:|---:|
| unique visited cells | 5 | 5 |
| revisited cells | 0 | 0 |
| horizontal-used cells | 2 | 4 |
| vertical-used cells | 5 | 4 |
| both-axes-used cells | 2 | 3 |
| legal successors | 3 | 2 |
| must-pass lower bound | **33** | **32** |

The raw move differs, but the frozen observer's relevant finding is that the branch also changes bounded path-resource occupancy and the relational must-pass bound.

## Divergence point 2

`multi-pick-975` DEAD moves north from [10,7] to [10,6].  
`multi-pick-112` LIVE moves west from [10,7] to [9,7].

Again, all frozen global-accounting fields agree.

Local path-resource projection:

| field | DEAD | LIVE |
|---|---:|---:|
| unique visited cells | 5 | 5 |
| revisited cells | 0 | 0 |
| horizontal-used cells | 3 | 4 |
| vertical-used cells | 4 | 2 |
| both-axes-used cells | 2 | 1 |
| legal successors | 2 | 1 |
| must-pass lower bound | **33** | **34** |

The must-pass lower-bound direction reverses between the two examples, so the result is **not** “smaller bound means LIVE.” What recurs is that a plain branch choice changes a future-relevant relational distance/resource interface even though scalar obligation counts and current hard facts remain equal.

That distinction matters. It nominates an interface family, not a threshold rule.

## Interpretation

Lane E's tested negative remains intact. The exact causal point is still ordinary divergence; this observer does not resurrect a hidden later commitment.

What changes is the description of the divergence. In both independent cases, the causally fatal move is not merely a different coordinate and not a named mechanic event. It changes how already-used path axes/resources sit relative to future must-pass completion cost.

This is compatible with a dependency-conditioned view in which ordinary geometric moves acquire significance through the future obligations they constrain.

But the current evidence is one parent. It cannot establish recurrence across boards, a reusable descriptor, or a production policy.

## Earned next step

Freeze the **same** bounded projection on independent parents:

- path-resource occupancy counts;
- legal successor count;
- the already-existing relational lower bounds;
- matching global-accounting controls;
- current hard-prune response.

Do not feature-shop after seeing independent outcomes.

Prefer retained exact matched pairs. If none satisfy a comparable causal-divergence contract, this nomination can remain capability memory until another independently justified acquisition naturally supplies them.

## Not earned

This result does not earn:

- a dependency-conditioned repair neighborhood;
- adaptive destroy size;
- backjumping;
- CP-LNS/LNS;
- a new hard prune;
- a fitted LIVE/DEAD classifier;
- exact-cell memorization;
- a broad exact-label campaign.

A production consumer requires independent-parent recurrence and then its own bounded matched-work falsifier.
