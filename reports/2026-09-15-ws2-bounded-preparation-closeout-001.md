# WS2 bounded preparation closeout 001

> **Status:** inconclusive
> **Last evidence:** 2026-09-15 — bounded local Class-4 attempt and offline Class-5 exact-evidence join
> **Decision:** Class-5 existing exact evidence is contrast-starved; the open-path observer is not promoted into routing. The Class-4 canary remains the first execution gate because the local production-shaped attempt was deliberately stopped before completion rather than converted into partial evidence.
> **Remaining gate:** complete the frozen matched Class-4 canary in adequate parallel compute and stop on every non-advance verdict
> **Base:** `main@874b314` (merged PR #1804)
> **Compute boundary:** no 113-row Class-4 population, Class-2 A/B/materialization solve, new exact labels, or standing human-parent corpus.

## Class 4: bounded attempt, no verdict

The frozen population was exactly the eight freshness IDs (`R00082`, `R02173`, `R02807`, `R03365`, `R00466`, `R03228`, `R00329`, `R03303`), `R01273`, and two non-portal controls (`R00001`, `R00039`). Both arms used the production scheduler, 50M initial nodes, 67M initial work, a nonbinding 24-hour safety deadline, one identical funded dead-last shell, lifecycle/attempt telemetry, and differed only on the retry-local treatment selector.

The local two-core container made the unsolved control shell materially more expensive than this closeout's low-cost preparation boundary: after 54 minutes the control had completed only the two non-portal rows, and the concurrently started treatment had completed only `R00001`. Both processes were terminated. Those partial rows do not cover a freshness row, are not analyzed as a canary, and are not retained or represented as decision-bearing v3 evidence. This is an incomplete execution, not a stop/advance verdict. The 113-row population was not run. A suitably parallel execution environment must rerun both complete arms and feed them to `ws2-experiment-readiness.mjs class4-canary` before any Class-2 or Class-4 population compute.

## Class 2: not materialized

The retained production rows predate #1804's canonical `class2ControlEligibility` projection. Producing the required current control-side participant evidence would require the broad control solve campaign explicitly excluded by this task. No 60-row cohort was fabricated from guessed fields and no treatment outcomes were inspected.

## Class 5: existing evidence is contrast-starved

The offline evaluator joined the research-only open-path observation to the existing exact-labelled B2 extinction-prefix evidence only. It admitted comparisons solely within the same level, endpoint-geometry identity, and board-obstacle reference-set identity, while retaining portal abstention.

The existing material contains 28 exact-labelled rows across 26 controlled strata, but **zero natural LIVE/DEAD contrasts** within those strata. Therefore there is no defensible observation on representation separation: zero eligible contrasts and zero separated contrasts. Verdict: `stop-contrast-starvation`. No new labels or decision-bearing human-parent families were generated, and the observer remains research-only.

## Preparation and salvage

- Added an inactive compact known-prefix oracle-set manifest helper. It canonicalizes packed or coordinate paths, hashes the stable full path set and explicit conditioning subset, records producer/version and compact source/parent/dependence metadata, and rejects malformed identities, paths, parents, or conditioning references. It is not wired into a solver or producer until a real decision-bearing prefix-survival experiment needs it.
- A development-only toy execution used published parent `P00002`, `local-mutant`, `count=1`, seed `20260915`, and temporary output only. It accepted `F00002-lm-01` on the first attempt by moving one block from `(3,4)` to `(4,7)`, retained strict inventory and the exact-coordinate inherited witness, emitted distinct parent/variant content hashes plus the family/run manifest, and recorded `solverOutcomeFiltering: false`, witness-certificate-only interpretation, and `parent-family` independence in the research context. The temporary family was deleted after inspection; it is not a standing corpus or Class-5 evidence.
- A repository-wide manifest scan found no present, unretained `pathfinder-solver-experiment-result` v3 manifest marked `decisionBearing: true`. Nothing could validly enter the existing durable-retention path without rerunning or cosmetically upgrading legacy evidence, so nothing was added.

## Handoff

1. Run the complete frozen Class-4 matched canary in adequate parallel compute; analyze and retain only a complete valid v3 bundle. Stop on every verdict other than `advance-to-113`.
2. Do not materialize Class 2 until current control-side eligibility exists without an unauthorized broad solve.
3. Treat Class 5's natural evidence as contrast-starved. A future tiny human-parent intervention requires a separately justified question and exact labelling; this closeout does not authorize it.
