# Post-merge moonshot recovery and prioritization 001

> **Status:** documentation/research-priority reconciliation
> **Date:** 2026-09-16
> **Authority boundary:** `docs/solver-optimization-workstreams.md` remains the live queue authority; `docs/solver-future-work.md` remains the deferred/reopen authority.

## Why this reconciliation was needed

After the September 16 merge consolidation, PR #1817 (`Reconcile recovered solver architecture avenues`) was closed rather than merged. Main retained the earlier recovered-avenues reconciliation report and some deferred descendants, but did **not** retain the full assumption-breaking moonshot harvest or the later indexing/prioritization of its unique ideas.

At the same time, main advanced materially beyond the tree on which the moonshot pass was written:

- Card-E sizing completed on an independent 156-row population and its tested state-selection descriptors closed negative after depth control;
- a controlled same-board/same-endpoint Class-5 topology pilot now has a frozen preflight;
- H1 relational event-feasibility now has a frozen prespec.

Therefore the correct action was not to restore PR #1817 verbatim. The ideation history was recovered intact, while the live priority docs were reconciled against the newer evidence.

## Recovered material

The full non-authoritative report `2026-09-16-assumption-breaking-solver-development-moonshots-001.md` is restored. It preserves twelve architecture/research families plus the declarative-mechanic cross-cutting idea:

- minimal DEAD cores / conflict-driven search;
- CEGAR obligation planning;
- bounded cold exact proof services;
- complete-path large-neighborhood search;
- backward abstraction / meet-in-the-middle feasibility;
- separator contracts / region decomposition;
- behavioral-state quotient / future-language equivalence;
- optionality search;
- interpretable program synthesis as a hypothesis engine;
- adversarial counterexample generation;
- cooperative blackboard portfolio;
- per-level search-plan compilation;
- declarative mechanic automata.

Nothing in that report is a production authorization.

## Current priority after reconciliation

The active capability-acquisition order is now:

1. frozen controlled open-path topology contrast pilot;
2. frozen H1 relational event-feasibility prespec;
3. minimal DEAD-core / minimum-relaxation diagnosis if the first two expose DEAD contrasts without a compact causal relation;
4. H2 structural-stasis observer;
5. H3 dependency-defined repair observer;
6. behavioral-state quotient probe;
7. separator/decomposition census;
8. cross-attempt basin overlap only for a real allocation question;
9. forced-chain census only after profiling shows material dynamic cost.

This ordering reflects evidence maturity. It deliberately does **not** let more radical architecture work leapfrog already-frozen, cheap, decision-bearing experiments.

## Future-work nursery

`docs/solver-future-work.md` now carries an explicit assumption-breaking nursery with reopen conditions. The most important preserved independent descendants are:

- complete-path LNS as a genuinely different search object;
- adversarial counterexample generation as a research-acceleration method;
- interpretable program synthesis as automated premise nomination;
- behavioral-state quotienting as a route to discovering the right state equivalence relation;
- separator/AND-OR decomposition as an alternative factorization of hard levels;
- cooperative blackboard exchange only after reusable cross-solver artifacts show value;
- per-level architecture compilation only after complementary solver architectures actually exist;
- declarative mechanic automata only after repeated architecture work demonstrates shared contract value.

H1 optionality, CEGAR planning, backward abstraction, learned nogoods, and bounded exact production queries remain descendants of more primitive evidence and are not duplicated as immediate projects.

## Protocol clarifications retained

Two assumption-breaking protocol conclusions are now explicit in the live authority:

1. **Exact computation is not itself forbidden in production.** Historical exact labels, known solutions, identity/provenance leakage, and stored witnesses are forbidden runtime steering inputs. A generic current-level/current-state exact computation could become legal only after soundness, bounded cost, and end-to-end economics are demonstrated.
2. **Radical architectures enter through cheap falsifiers.** Census, observer, tiny-state laboratory, or economics replay first; implementation-shaped queue debt only after the phenomenon exists.

## Net result

No moonshot idea from the closed branches is now silently lost. Every idea is either:

- represented in the current active premise stack;
- nested under an already-live premise such as H1/H3;
- preserved with an explicit reopen condition in `solver-future-work.md`; or
- retained in the full moonshot harvest as non-authoritative architecture history.

The active queue remains conservative despite the expanded idea space.