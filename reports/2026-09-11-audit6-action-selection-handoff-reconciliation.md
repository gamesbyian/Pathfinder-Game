# Audit 6 action-selection handoff reconciliation — 2026-09-11

> **Status:** concluded-positive
> **Last evidence:** 2026-09-11 — the current post-1,029 residual atlas report and its class-1 never-offered population, which superseded this branch's stale 8/9 four-beam nomination.
> **Decision:** preserve the fixed-total-work menu-validation requirement, but do not launch the stale 8/9 four-beam experiment named in this branch's closeout checklist. Newer merged evidence rebuilt the current class-1 population at the post-1,029 boundary and nominates a different compact beam menu.
> **Remaining gate:** after the active joint-obligation/orchestration work is reconciled, run the current class-1 report's prespecified bounded, level-blind treatment under a fixed total-work envelope before any production-menu promotion.
> **Audit area:** 6 — Technique / action selection post-closeout handoff

## Recovery finding

The audit branch diverged before later `main` work closed the current class-1 missed-rescuer analysis. The branch ledger's Audit 6 row and hardening checklist refer to an earlier nine-level view and this four-beam menu:

- `objectiveFirst@5000`;
- `perimeterSweep/perimeterCW@2000`;
- `perimeterSweep/perimeterCCW@2000`;
- `intersectionHarvest@2000`.

That is no longer the best available population evidence.

Merged commit `36c231a0f82a34bb8781ad04464182ca8052c6b4` added `reports/2026-09-11-class1-missed-rescuer-menu-audit-001.md` from corrected static audit run `34559811913` on the post-1,029 residual atlas. It finds 26 current class-1 levels and 50 actually missed winner rows. Its compact four-beam nomination covers 17/26 levels:

1. `beam|score=intersectionHarvest|bias=none|width=5000|retention=mechanic-buckets`;
2. `beam|score=knotBuilder|bias=none|width=2000|retention=plain`;
3. `beam|score=intersectionHarvest|bias=none|width=2000|retention=plain`;
4. `beam|score=objectiveFirst|bias=none|width=5000|retention=mechanic-buckets`.

A fifth perimeter-CW beam adds only one more selected level and is explicitly a second decision, not part of the first experiment.

## Correct handoff

The audit finding remains valid in substance: production has a bounded action-exposure opportunity, and isolated historical winners must not be promoted directly. The next treatment must therefore:

- freeze the then-current production control and residual population;
- expose a fixed, prespecified compact menu through a level-blind rule, never exact level IDs or stored winner lookup;
- keep the candidate inside an explicit fixed-total-work/additive-work envelope;
- record offer, dispatch, nonzero participation, work, solve contribution, unique gain and regressions;
- use the selected class-1 rows only as development/participation evidence;
- require independent population evidence before production promotion;
- preserve specialist coverage rather than converting historical winner counts into an unconditional larger portfolio.

## Concurrency / ownership guard

The newer class-1 report explicitly defers implementation while the active joint-obligation branch changes orchestration/pruning. This audit branch should not introduce a competing routing/orchestration mechanism in parallel. Once that lane is reconciled, the current class-1 report is the decision authority for the first bounded menu experiment.

## Disposition

Close the Audit 6 post-closeout *handoff* item as reconciled. Do not mark the candidate itself promoted or experimentally confirmed. The remaining experiment belongs to the live Workstream 1 queue, using the newest residual boundary and menu evidence available when it actually runs.
