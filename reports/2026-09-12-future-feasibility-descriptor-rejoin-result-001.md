# Future-feasibility descriptor rejoin result 001

> **Status:** concluded-negative
> **Last evidence:** 2026-09-12 — 31 exact-labelled states across 15 levels: every resolved case in the committed B2 extinction set after the flipping-filter rerun plus the frozen `R03229` microscope cases.
> **Decision:** none of the bounded exact-resource/topology/joint-obligation summaries earns a runtime treatment. Do not expand into post-hoc feature accretion from this result.
> **Remaining gate:** none for these six summaries. A materially different premise is required; current WS2 diagnostic successor is compact sound dead-cause recurrence before any solve-local nogood implementation.

## Result

The six candidates frozen in `2026-09-12-future-feasibility-descriptor-rejoin-preflight-001.md` all overlap between exact-LIVE and exact-DEAD states.

| Descriptor | LIVE mean | DEAD mean | Exact value separation? |
|---|---:|---:|---|
| `intersectionCapacitySlack` | 0.96 | 5.50 | no |
| `freeIntersectionSlackAfterMustCross` | 4.56 | 1.67 | no |
| `pendingLowDegree2` | 0.52 | 2.00 | no |
| `minPendingDegree` | 2.75 | 2.17 | no |
| `activeJointClusters` | 0.04 | 0.33 | no |
| `jointRejects` | 0.00 | 0.00 | no |

The DEAD states do show some directional tendency toward lower residual freedom / greater scarcity on averages, but the overlap is extensive and the values fail the prespecified mechanism-specific distinction bar. In particular:

- optimistic intersection-capacity slack overlaps heavily and sometimes assigns *more* slack to DEAD states than their LIVE siblings;
- reserving intersections for pending must-cross obligations is largely progress bookkeeping and does not separate feasibility;
- low-degree pending-obligation counts have the expected average direction but overlap at every useful threshold;
- the existing observer-only joint-obligation evaluator is mostly inactive on this population and emits **zero reject verdicts** across all 31 labelled states.

`R03229` remains illustrative rather than rescuing a descriptor: its LIVE witness has `pendingLowDegree2=3` while both DEAD retained survivors have `5`, but the broader B2 population contains LIVE states at the same or overlapping scarcity values. That is not a generic discriminator.

## Interpretation

This closes the current **simple compact future-feasibility summary** route, not the underlying diagnosis. B1/B2 + `R03229` still establish the important mechanism fact: current rank can prefer exact-DEAD material over exact-LIVE alternatives. What failed is the attempt to explain that distinction with these six cheap runtime-legal summaries.

The result also sharpens the conceptual lesson from the bucket canaries: retaining progress diversity and counting coarse remaining opportunity are both insufficient proxies for actual viable continuation. No additional descriptor family is authorized by this null.

## Next architectural implication

A subsequent premise should change what information is represented or reused rather than adding nearby scalar score features. The recovered future-work line **reason-producing dead-state reuse / solve-local nogoods** is therefore more relevant as a *different architectural hypothesis*, but it remains gated: first demonstrate material recurrence of the same sound compact dead cause within solves. This result alone does not activate that implementation.

## Reproducibility

Read-only script: `scripts/stress/future-feasibility-descriptor-rejoin.mjs`.

The script replays prefixes through production search-state primitives, uses the existing joint-obligation evaluator, and never feeds exact labels or descriptors back into search. B1 remains historical precedent; the executable population uses B2's committed case file because its resolved labels and post-flipping-filter rerun are directly reconstructable, plus `R03229`'s frozen cases.