<!-- agent-context-budget: warn=10500 max=14000 -->
# Solver optimization workstreams

> **Status:** canonical live authority for solver research priority, state, and next gates.
> **Reconciled:** 2026-09-17.
> **Scope:** improve cold level-blind solve count and/or machine-independent work while protecting correctness/generalization.

Method: [`solver-research-operating-model.md`](solver-research-operating-model.md). Scheduling: [`solver-scheduling-policy.md`](solver-scheduling-policy.md). Evidence: [`solver-evaluation-evidence.md`](solver-evaluation-evidence.md). Capability memory: [`solver-capability-memory.md`](solver-capability-memory.md). Semantic capability map: [`solver-reasoning-capability-atlas.md`](solver-reasoning-capability-atlas.md). Historical stop-condition reconciliation: [`solver-capability-gap-stop-condition-reconciliation.md`](solver-capability-gap-stop-condition-reconciliation.md).

Program lens: **capability composition** exposes/selects/allocates demonstrated capability; **capability acquisition** creates generic capability where no known action succeeds. The current emphasis is acquisition.

## Data-audit guardrail

Corpus 1 is not cross-generator transfer; Corpus 2 is a mixed development laboratory. Residual/participant claims are population-conditional. Level-blindness constrains provenance, not specificity: a generic cold procedure may derive exact board-specific facts from the current puzzle/current invocation, but may not consume historical identity/outcome/hint/capability-memory membership. A historical negative closes only the semantic/form/generalization claim actually tested.

## Current execution priority

### 1. Workstream 2: residual capability + premise acquisition

**State:** ACTIVE / FIRST PRIORITY.

**Production boundary:** run `35066677597` is **101/102 C1 + 1,169/1,700 C2**, residual 531; atlas Classes 1-5 = 17/30/23/71/390. [`refresh`](../reports/2026-09-16-post-promotion-production-boundary-refresh-001.md)

**Class 1-3:** class 1 has no menu headroom; class 3 is exposed/dispatched but **dose-unverified**, not proven comparable-work negative. Must-turn-biased late repair is **CLOSED NEGATIVE** (60 rows, zero gains, +6.5% work). [`lineage`](../reports/2026-09-14-historical-claim-lineage-audit-001.md) · [`economics`](../reports/2026-09-16-class2-must-turn-economics-result-001.md)

**Class 4: PROMOTED.** Dead-last portal coarse-state retry reproduced **86 gains / 0 losses** on 113 allocation rows and is default ON. Global portal coarse merge remains closed. [`promotion`](../reports/2026-09-16-class4-113-allocation-promotion-001.md)

**Class 5:** controlled same-board/same-endpoint forks produced 4 exact-resolved LIVE/DEAD-discordant pairs across 2 human parents. Topology is therefore an earned microscope premise, not a production key. [`result`](../reports/2026-09-16-class5-controlled-topology-fork-pilot-result-001.md)

**Completed post-topology hypotheses:** H1 closed its tested compact event vocabulary; DEAD-core is population-limited; H3 repair-commitment interface and H2 stasis observer closed negative; behavioral quotient is mixed/capability-memory only. These outcomes narrow forms, not every broader semantic operation they touched.

### Premise-acquisition lanes

The queue is intentionally **not one long serial staircase**. Cheap retained-evidence/offline premise tests may run in parallel. Expensive production treatments remain gated behind a positive premise.

#### Lane A — separator / decomposition

1. **Separator/decomposition census — RESULT PENDING MERGE.** PR #1827 reports a bounded positive on current Class-5: 121/390 (31.0%) with balanced static/mechanic-aware separator width <=4; portal-mediated interfaces near-null. Treat this as pending until merged.
2. **If the census lands positive:** immediately measure **interface-contract state size**, not another separator count. Ask whether path/mechanic history crossing a narrow separator compresses into a small enough exact/conservative boundary contract to support region composition. A small contract earns one bounded decomposition prototype; state explosion closes the route before implementation debt.
3. Path-history-conditioned separator questions may reuse the fresh exact sibling asset/frozen prefixes rather than launch a separate broad corpus run.

#### Lane B — fresh exact LIVE/DEAD asset + causal cores

1. **Fresh exact LIVE/DEAD sibling asset — EARNED / PREPARED.** Harvest the residual-selected, label-blind sibling population in [`solver-fresh-dead-sibling-harvest-preflight.md`](solver-fresh-dead-sibling-harvest-preflight.md).
2. First consumer remains DEAD-core size/specificity because B2 exhausted its tiny population rather than falsifying the premise.
3. The same frozen asset may support separately prespecified topology, relational-feasibility, and revision microscopes. Do not retrofit hypotheses after seeing labels.
4. A per-instance core/conflict need not recur across levels. Recurrence is required only for a fixed reusable clause/descriptor claim.

#### Lane C — solve-local typed knowledge reuse

**OPEN / PREPARED.** Execute [`solver-solve-local-rediscovery-preflight.md`](solver-solve-local-rediscovery-preflight.md).

Start with retained connectivity-rejection evidence. August Stage B found strong within-level abstraction recurrence (83.1% reached-shape sharing vs 52.6% literal exact-state sharing) but closed the cross-level checker because only 8.8% of records belonged to cross-level-spanning shapes. Under the corrected level-blindness contract that leaves a distinct solve-local premise.

Gate order:

1. determine whether repeated expensive facts cross branches/attempts/stages far enough apart to matter;
2. identify a **sound cheap lookup key/certificate** that does not require rerunning the expensive derivation;
3. price reuse against derivation/lookup cost;
4. only then build one typed within-invocation memo/handoff pilot.

No generic blackboard/framework until one concrete fact family pays.

#### Lane D — per-instance relational feasibility

**OPEN / PREPARED.** Execute [`solver-per-instance-relational-feasibility-preflight.md`](solver-per-instance-relational-feasibility-preflight.md).

This deliberately reopens semantic questions whose historical implementations or descriptor forms were narrower than the premise:

1. **future-intersection commitment realizability** on matched LIVE/DEAD states;
2. **constrained-event feasibility** through a nominated crossing/region/interface/portal/approach class;
3. **residual-interface commutativity** by actual swap/replay/exact feasibility, not obligation-multiset similarity.

Use existing exact-labelled states first, then the fresh sibling asset. H1's null blocks its frozen event vocabulary, not a generic bounded per-instance query. Historical blueprint behavior must not be restored unchanged.

A positive query earns only its cheapest consumer: prune/forced move/lower bound/decomposition/conflict/exact-mode switch. `UNKNOWN` is always non-decision-bearing.

#### Lane E — dependency-defined causal revision

**OPEN / PREPARED.** Execute [`solver-dependency-defined-revision-preflight.md`](solver-dependency-defined-revision-preflight.md).

Broad positional/prefix repair is already weak. The changed premise is whether a later DEAD near-miss is caused by a **small dependency-defined earlier commitment set** rather than path-distance rollback.

First observer: on exact-labelled repair-retreat/dead-vs-rescuable cases, identify earliest divergent future-relevant commitments across turn choices, crossing axes/placement, portal/flipper order, obligation order, separator side/topology, and visit/edge-use facts. A compact causal interface earns one targeted revision/backjump/local-surgery prototype at equal work. Whole-prefix blame closes the route.

#### Lane F — bounded exposure / representation reconciliations

1. **Class-3 exact-action dose reconciliation — BOUNDED REOPEN.** Only if exact participation/work is cheaply recoverable. Do not run a broad campaign merely to complete a table. Systematic under-dose creates an allocation premise; verified comparable dose closes the ambiguity.
2. **Card-E quotient hypothesis discovery — DISCOVERY ONLY.** One small parent-held-out interpretable interaction search may try to turn the mixed quotient result into a named legal hypothesis. No fitted runtime classifier.
3. **Topology per-instance microscope — OPEN, preferably joined to Lanes A/B/D.** Seek a sound actionable consequence such as separator-side commitment, path-conditioned accessibility, or topology-aware impossibility/equivalence. Do not hunt another universal raw phase feature.

#### Lane G — independent search-object nursery

1. **Complete-path LNS cheapest falsifier — OPEN.** Test whether plausible relaxed complete paths are structurally/edit-distance close enough to accepted solutions for local surgery to be credible. Positive evidence earns a prototype; null closes this route cheaply.
2. **Backward/bidirectional abstraction remains deferred** until a compact sound residual/interface signature emerges from A/D/topology. Do not reopen full MITM.

### Queue ordering and parallelism

For a single agent, prefer: **A result reconciliation -> B fresh sibling asset -> D relational-feasibility queries -> C solve-local rediscovery -> E causal-revision observer -> F reconciliations -> G LNS falsifier.**

For multiple agents/resources, **C, D, E, F and the LNS falsifier may run in parallel** because their first gates are retained-evidence/offline observers and do not depend on changing production policy. Lane A's downstream contract-size work waits for the census result to land; Lane B's fresh asset may later strengthen D/E but is not required for their existing-data first pass.

**No production treatment is currently earned by the new lanes.** Their purpose is to open premise gates cheaply and convert positives into narrowly specified treatments.

### 2. Workstream 1: automatic solver action selection

**State:** SUPPORTING / NO ACTIVE PRODUCTION SELECTOR GATE.

Use capability/lifecycle/provenance/family/trace evidence to distinguish not-exposed from exposed-and-failed. Production selectors need legal current-level/current-solve signals and confirmation proportional to selection pressure. The September structural-response ladder already ran through first divergence; do not restart earlier stages.

A result from Lane D, E, or Card-E discovery may reopen WS1 only if it yields a cheap current-input signal predicting which genuinely different action should receive work. Do not turn exact offline labels or historical family membership into routing features.

## Workstream state

| ID | Workstream | State | Next gate |
|---:|---|---|---|
| 2 | Residual capability + premise acquisition | **ACTIVE / FIRST** | Run parallel bounded lanes A-G; fresh exact siblings and relational-feasibility are central capability-acquisition gates |
| 1 | Automatic action selection | **SUPPORTING** | Reopen from a new legal current-input response signal produced by the acquisition lanes |
| 6/7 | Repair reachability / architectural speed | **SUPPORTING** | Lane E tests dependency-defined revision; fresh profiling gates pure speed work |
| 3 | Generalization | **METHOD COMPLETE** | Preserve independent units and scale confirmation with selection pressure |
| 8 | Isolated capability | **SUBSUMED BY WS1** | Isolated winners are selection evidence, not tail entitlement |
| 0/4 | Restart/randomization / beam retention | **CLOSED IN TESTED FORMS** | Reopen only for changed recurring mechanism evidence |
| 5 | Exact/reference model | **ON DEMAND / BUSIER** | Truth/query service for fresh siblings, DEAD cores, relational feasibility, topology and revision microscopes |

## Standing research rules

- Use `workSpent` across techniques; nodes are within-technique diagnostics. New actions/configs normally compete inside total work.
- IDs, historical outcomes, hints, family labels, stored exact answers, and capability-memory membership never become cold runtime routing inputs. Current-input exact derivation is legal if sound and economical.
- Separate **semantic premise** from **tested form**. Classify old stop conditions before inheriting a negative: form-closed, descriptor-closed, cross-level-closed, participation-invalid, dirty-revert, population-limited, economics-closed, semantic-closed, or open-per-instance.
- Exact/offline evidence can falsify or nominate a production premise; it does not itself license a runtime mechanism.
- Reusable rows require matching protocol identity; nominal stage reach is not participation. Comparable-work negatives require exact-action work/dose evidence. Timeout/errors are indeterminate.
- A single-level microscope may generate a premise, never a production exception. A board-specific derived fact may still be legitimate output of a general procedure.
- Prefer the cheapest information-value test before implementation. Radical architectures enter through censuses, exact-labelled observers, tiny-state laboratories, retained-evidence joins, or economics replays.
- Hold out independent units and scale confirmation with tuning/selection pressure. For human/editor descendants the parent is usually the independent unit.
- For hard consumers, prove soundness. A useful signature/correlation is not an impossibility certificate or state equivalence.
- A generic learned store/blackboard, production exact solver, CEGAR engine, LNS system, decomposition engine, or per-level compiler is **not** earned merely because its semantic gap is open. First positive premise -> one smallest consumer/prototype -> matched-work economics -> broader architecture only if needed.
- Reconcile old questions before new compute; search forward from exact historical vocabulary before declaring an idea untested.

## Cheap evidence routing

Use `research-status-index --compact`, `tooling-census --compact`, `research-asset-query`, `corpus-query`, capability-memory/provenance helpers, audited family tooling, and retained exact-labelled artifacts before bespoke work. Chronology and measurements belong in dated reports; deferred descendants belong in [`solver-future-work.md`](solver-future-work.md).
