# Solver optimization workstreams

> **Status:** canonical live entry point for solver capability and efficiency research.
> **Reconciled:** 2026-09-01 after completion/sealing of the naming cleanup and post-Phase-15 solver-research resumption hardening.
> **Scope:** improve cold level-blind solve count and/or machine-independent work while protecting correctness and generalization.

This file owns **current execution priority, workstream state, and next gate**. Workstream IDs are stable historical identifiers, **not ranks**. Detailed reasoning belongs in linked docs/reports.

Primary authorities: research method [`solver-research-operating-model.md`](solver-research-operating-model.md); scheduling/allocation [`solver-scheduling-policy.md`](solver-scheduling-policy.md); residual representation [`solver-residual-state-representation.md`](solver-residual-state-representation.md); deterministic cost [`solver-budget-determinism.md`](solver-budget-determinism.md); level-blindness [`solver-level-blindness.md`](solver-level-blindness.md); retained opt-ins [`solver-opt-in-experiment-ledger.md`](solver-opt-in-experiment-ledger.md); deferred/reopen ideas [`solver-future-work.md`](solver-future-work.md).

Naming-migration handoff: Phase 15 and its hostile-audit repair are complete. Frozen reports remain evidence in their original vocabulary, not current executable runbooks. Before decision-bearing solver work resumes, follow [`solver-research-post-naming-resumption.md`](solver-research-post-naming-resumption.md) and pass `npm run test:solver-research-resumption`.

## Workstream-wide rules

Every active item follows **premise → smallest value-of-information pilot → explicit success/stop gate → bounded implementation → confirmation → broader integration**.

- Use `workSpent` for cross-technique allocation; raw nodes are within-technique diagnostics. `baseWorkBudget` is the preferred name for deterministic base allocation; `strictTotalWorkBudget` is the current true whole-solve work envelope.
- New actions/configs expand the menu, not the default total budget.
- Selected/tuned treatments need independent confirmation proportional to selection pressure; cross-generator transfer is reserved for broad/high-selection-risk claims. See [`solver-evaluation-evidence.md`](solver-evaluation-evidence.md).
- Level-blindness is not generalization.
- A clear negative closes the tested form unless materially new evidence changes its premise.
- Prefer locked confirmation pools partitioned into one-use blocks when repeated selected candidates are expected. Spend the block whose outcomes influence design; untouched sibling blocks remain available. A fresh one-off cohort is still fine when cheaper.
- Major capability refreshes should emit an output-only delta digest: gain/loss IDs, winner stage/action, per-level work/attempt deltas, and aggregate work split across prior solves/new solves/still-unsolved.

## Immediate execution priority

### Current post-naming Workstream-2 handoff

This is the short current-state view; the long Workstream-2 row below intentionally preserves the chronology that produced it.

- **EW1 is complete.** Do not repeat or enlarge the 60-level equal-work pilot merely because earlier chronology below says the plan tier was once deferred.
- **Next empirical gate:** obtain a production report from the exact current solver commit with `--lifecycle-telemetry` and per-attempt `workSpent`, then run `npm run solver:analyze-equal-work-production-reach -- --equal-work=<EW1 combined-cells.json> --production=<current report> --require-current-head --check`. The analyzer accepts the maintained raw level-blind `{summary:{commit,corpus},levels}` wrapper as well as flattened `{commitSha,corpus,levels}` stress/combined reports, canonicalizes historical/current attempt, action, and stage identities at ingestion, and refuses decision-bearing status when commit/corpus provenance or required telemetry is incomplete.
- Use that joined development view to prespecify the smallest production-shaped static repricing A/B. The A/B itself uses a fixed whole-solve envelope (`strictTotalWorkBudget`) and follows the normal selection/confirmation rules.
- Workstream 1 remains downstream of this resource-model/repricing gate. Do not mine another nearby missing-exposure placement rule while this handoff is open.

Current ordered execution priority:

1. **Workstream 2 — Budget-model completion + evidence-driven fixed-work scheduler repricing.** This is the current implementation priority and default place to spend solver-infrastructure effort.
2. **Workstream 1 — Automatic solver action selection.** Resume only from a materially new routing/allocation premise using Workstream 2's corrected work model; the two nearby standard-width IH exposure forms are closed.

Workstreams 6 and 7 remain active secondary/supporting programs, but they remain below the sequence above in current execution priority. Workstream 0's prespecified production-equivalent restart form and cross-level learned-failure certificate direction are closed as tested.

Do not skip past Workstream 2 to lower-priority speculative solver work merely because the budget migration is less glamorous. The remaining budget debt can invalidate or muddy later scheduler comparisons by letting a nominal wall deadline resize search. Clearing it is research-enabling work, not cosmetic architecture.

Within Workstream 2, use this order unless fresh evidence changes it:

1. **Ownership/parity first:** finish explicit work ownership (`BudgetEnvelope` projection, attempt/stage budget context, session-isolated multi-solve work accounting) with no intended solve-policy change.
2. **Equal-work evidence substrate:** extend isolated/census tooling where scheduler pricing still relies on node-depth-only evidence.
3. **One additive tier at a time:** replace ms-derived dose with an explicitly equivalent work dose, prove parity under the current policy, then remove that site from the CI allowlist.
4. **Whole-solve invariant:** when the legacy inventory is gone, widen deadline-independence tests from the main ladder to the full production solve.
5. **Then reprice:** only then treat work quanta/tranches as scheduler policy variables under a fixed total-work envelope.

If an ostensibly structural conversion changes solved sets, attempt order, seeds, or material `workSpent`, stop classifying it as migration and route it through the scheduler experiment/confirmation pipeline.

## Active workstreams

Rows are sorted by stable workstream ID, not by execution priority.

| ID | Workstream | State | Next gate |
|---:|---|---|---|
| 1 | Automatic solver action selection | **PROMOTED DEFAULT-ON (2026-08-27)** | Post-976 rejoin still shows **73 not-offered**, **57 starved**, and **9 adequate-depth non-replay** current misses with an observed isolated base solver. The selective mechanic-bucket-retention IH treatment chosen from this mined seam was +9/-0 on Corpus 2 and 0/0 on fresh `confirm-broad-002` — that exact rule stays closed. A newly prespecified, one-dimensional, fixed-envelope descendant — `STRATEGY_MUSTCROSS_FLIPPER_WIDE_BEAM_EXPOSURE`, exposing plain-retention `beam|score=intersectionHarvest|bias=none|width=5000|retention=plain`/`beam|score=objectiveFirst|bias=none|width=5000|retention=plain` to only the must-cross+flipper-heavy sub-rule — was +3/-0 on a 486-level `must-cross-heavy` routing-regime-sample development A/B, and **has now also passed real broad confirmation**: `confirm-residual-003` (516-level control-failure residual, run [`33054538000`](https://github.com/gamesbyian/Pathfinder-Game/actions/runs/33054538000), recombined via one-shot run [`33083577386`](https://github.com/gamesbyian/Pathfinder-Game/actions/runs/33083577386) after an unrelated glob bug) came back control 0/516, treatment 3/516, work +0.15%, gained `L00278`/`L00831`/`L00933`, lost none — a clean gain matching the development A/B's shape. **`confirm-broad-003`, `confirm-broad-004`, `confirm-residual-001`, and `confirm-residual-002` were all control-vs-control, not control-vs-treatment.** `.github/workflows/solver-broad-confirmation.yml` and `solver-residual-confirmation.yml` referenced `matrix.arm` (nonexistent — the shard plan nests it under `matrix.shard.arm`) in the one step that adds `--enable-flags`/`--disable-flags`, so every "treatment" shard of every dispatch of either workflow silently ran with no ablation flags at all, from each workflow's introduction through `confirm-residual-002`. The four previously-reported diagnoses (repair-fallback saturation twice, a main-search late-reserve scheduling gap, then a worker-concurrency-correlated node/work variance) were each a plausible read of a real-looking null result, but none of them explain what actually happened, because none of those runs ever had a real treatment arm to begin with. Full account: [`2026-08-27-confirmation-workflow-treatment-flag-wiring-bug.md`](../reports/2026-08-27-confirmation-workflow-treatment-flag-wiring-bug.md). Both workflows are now fixed (`matrix.shard.arm`) and hardened: every shard persists a `*-flags.json` artifact recording its resolved arm/flags, and a shard fails fast if a treatment arm resolves no flags or a control arm resolves any. **What still stands on independent grounds:** the 2026-08-26 `MAIN_LOOP_LATE_RESERVE_CONFIG_COUNT` WORK-budget carve-out fix (`orchestration.ts`'s `mainLoopEarlyWorkBudget`/`earlyGateWorkBudget`, PR #1506) closes a real NODE-vs-WORK reserve asymmetry, independently verified by two regression tests reproducing the starvation mechanism against synthetic budgets (red pre-fix, green post-fix) — it just was never the explanation for `confirm-residual-001`'s result. The worker-pool concurrency-determinism question this bug indirectly raised was genuinely, separately investigated and **closed negative**: real `--workers=4` concurrency does not change a level's deterministic node/work trajectory, validated locally and on the actual GitHub Actions production runner class across 258 attempts ([`2026-08-27-worker-pool-concurrency-determinism-diagnosis.md`](../reports/2026-08-27-worker-pool-concurrency-determinism-diagnosis.md)) — it just was never the explanation for `confirm-residual-002`'s result either. `STRATEGY_MUSTCROSS_RESERVE_WIDEN_BEAM_EXPOSURE`'s own development A/B used the separate Sample-A/B workflow family (`plan-ab-corpus-shards.mjs`, two independent dispatches with different `enable_flags` inputs, not a matrix `arm` field) and is **unaffected** by this bug: **0 gains / 0 losses with real nonzero work/node engagement** stands as a genuine clean null. **Closed negative**; do not reopen that exact form. **Closed as promoted:** `STRATEGY_MUSTCROSS_FLIPPER_WIDE_BEAM_EXPOSURE` is now **default-ON** in `modules/solver/attempts.ts` (removed from `OPT_IN_FEATURES` in `ablation-config.ts`; the flag's own application check flipped from `cfg && cfg.FLAG === true` to `!cfg || cfg.FLAG`, matching every other default-ON gate — see that file's own comment). Evidence chain: development A/B +3/-0 (486-level must-cross-heavy sample) → same-generator confirmation +3/-0 on an untouched control-failure residual (`confirm-residual-003`) → cross-generator topology-composition transfer attempt (`confirm-transfer-topology-001`, run [`33108351046`](https://github.com/gamesbyian/Pathfinder-Game/actions/runs/33108351046)): control 997/1000, treatment 997/1000, +0.42% work, 0 gained, 0 lost — a clean null, ceiling-limited by that sample's own 99.7% control solve rate, not evidence against the mechanism. Per `docs/solver-evaluation-evidence.md`'s promotion table, a "one prespecified narrow treatment with little/no tuning" may take a narrow default-on promotion after confirmation alone; the transfer attempt's zero losses added a no-harm signal on top of that. See [`2026-08-24-solver-confirmation-transfer-cohort-reservation.md`](../reports/2026-08-24-solver-confirmation-transfer-cohort-reservation.md) for the full record and [`solver-opt-in-experiment-ledger.md`](solver-opt-in-experiment-ledger.md)'s "Recently promoted" table for current disposition. `transfer-envelope-001` remains locked and useful as a same-generator **in-envelope challenge/confirmation** stratum for a future candidate. See [`solver-evaluation-evidence.md`](solver-evaluation-evidence.md). **2026-08-28:** a level-blind targeted sweep (GHA run `33140805411`) re-measured the exact 724-level residual `post-976 rejoin` analyzed, at `main`'s current head (the promotion above plus everything else merged since). Result: **3/724 new solves** (`R02151`, `R00817`, `R02010`, each confirmed previously `node-budget-reached` in the baseline), **721/724 still unsolved** at a 50,000,000-node budget. A real, level-blind, no-leakage gain, but small next to the rejoin's own 139-level actionable-residual estimate — this residual is still overwhelmingly open. **2026-08-28 missing-exposure follow-through:** the next standard-width IH seam was tested and is now closed. The append-last `STRATEGY_HIGHINT_STANDARD_INTERSECTION_HARVEST_BEAM_EXPOSURE` selected replay was +1/-0, but its prespecified 120-level mechanics-eligible strict-67M development A/B was **56/120 control vs 55/120 treatment, 0 gains / 1 loss (`R02965`)**, with real treatment participation on 68/120 rows. The loss exposed fixed-work displacement: adding a useful beam changed late-reserve allocation and starved an existing winner. A materially narrower reserve-preserving placement descendant retained the `R02440` rescue but still lost `R02965` in its selected mechanism replay (+1/-1), because even preserved suffix membership cannot make preceding work free. **Close both exact exposure forms.** The next Workstream 1 action should no longer be another nearby placement tweak mined from these rows; resume automatic solver action selection only from a materially new routing/allocation premise, using Workstream 2's corrected work model. [`post-promotion residual targeted sweep`](../reports/2026-08-28-post-promotion-residual-targeted-sweep.md), [`post-976 rejoin`](../reports/2026-08-25-post-976-portfolio-exposure-rejoin.md), [`976 reconciliation`](../reports/2026-08-25-capability-sweep-976-reconciliation.md), [`mustcross-flipper wide-beam development A/B`](../reports/2026-08-26-mustcross-flipper-wide-beam-exposure-development-ab.md), [`mustcross-reserve-widen development A/B`](../reports/2026-08-26-mustcross-reserve-widen-beam-exposure-development-ab.md), [`confirmation-workflow treatment-flag wiring bug`](../reports/2026-08-27-confirmation-workflow-treatment-flag-wiring-bug.md), [`cohort reservation`](../reports/2026-08-24-solver-confirmation-transfer-cohort-reservation.md) |
| 2 | Budget-model completion + evidence-driven fixed-work scheduler repricing | **ACTIVE FOUNDATION; COMPLETE BUDGET SEMANTICS BEFORE NEW REPRICING POLICY** | The two-DFS suppression candidate remains closed (+3/-2 on sealed confirmation), but the allocation program itself is now active for a stronger reason: the 2026-08-27 audit found that production `workBudget` is a base allocation, not necessarily a whole-solve cap, and a finite set of additive tiers still derives fresh work from `timeBudgetMs * fraction`. **Next gate sequence:** (A) finish behavior-preserving substrate first: make stage work ownership explicit, replace stale-cap-prone `prep._workCap` inheritance with explicit budget context, isolate multi-solve work ownership, and add equal-work research execution where node-depth evidence is currently insufficient; (B) migrate the inventoried ms-derived additive tiers one at a time to deadline-independent work expressions, preserving each tier's current effective dose/eligibility/order unless a separately declared policy experiment says otherwise; require whole-solve deadline-independence/parity evidence as the inventory shrinks; (C) only after the resource model is coherent, resume scheduler repricing under a fixed total-work envelope with residual/tranche evidence. Do **not** batch-convert all tails or smuggle solve-set changes into the structural migration. The CI budget-boundary ratchet must keep the legacy inventory non-growing. **2026-08-28:** (A)'s "isolate multi-solve work ownership" sub-item is now largely closed — `diversification.ts`'s hint-discovery session, `hint-ablation-generator.ts` (~30 sites, all 7 phases), and three of `hint-workbench.mjs`'s own steps all accumulate a caller-owned session-local work counter instead of reading the shared realm-global meter; only one non-binding hang-safety callback (`runEnumeration`'s `shouldStop`, no caller-visible per-step result to intercept) remains and is explicitly flagged in place. Separately, made a start on (A)'s "equal-work research execution" sub-item (also gate-sequence step 2's own text below): `technique-census-cell.mjs`'s `runCell` now supports an opt-in `cell.workBudget`, mirroring `method-probe.mjs`'s existing `--work-budget` mode — the execution primitive only, previously untested by any node-test/vitest, now covered by 9 new tests. Wiring an actual equal-work plan tier (population/scale) into `build-technique-census-plan.mjs`/`combine-technique-census-shards.mjs` remains deliberately deferred as its own decision-bearing research step, not plumbing. No production solve behavior changed. **2026-08-28, same day, (B) value-of-information pilot (observational, no code change):** [`additive-tier-participation-audit`](../reports/2026-08-28-additive-tier-participation-audit.md) found, first by static trace and then empirically, that all 9 CI-ratchet-approved ms-shaped additive tiers (plus `late-repair-search` / its multiseed retry) are already disabled in both real interactive production paths (`solver-controller.ts`/`review-controller.ts` pass `disableExtraBudgetPasses: true`, which zeroes every one of their participation conditions) — migrating any of their formulas carries **zero live-play risk**. Their real relevance is entirely to offline capability-sweep/confirmation tooling, where a 10-level empirical run found they are neither rare (10/10 levels engaged at least one) nor cheap (per-tier `workSpent` ran 1.5x-467x the nominal `workBudget`, several tiers deriving from a fixed constant or `1.0`-fraction independent of the caller's own budget). This reframes step (B)'s priority: the higher-value near-term finding for anyone consuming a capability-sweep/confirmation number may be budget-legibility (`node_budget` inputs are not real per-level ceilings without `strictTotalWorkBudget`), not ms-vs-work rounding purity — worth weighing before picking which single tier to migrate first. **2026-08-28, same day: budget legibility documented, and `coarse-state-near-tie-retention-disabled-retry` migrated (Workstream 2 step 3's first site).** The `node_budget` input on `solver-broad-confirmation.yml`, `solver-residual-confirmation.yml`, `solver-level-blind-targeted-sweep.yml`, and `level-blind-capability-sweep.mjs` now states precisely, at the input itself, that it is a per-level starting allocation rather than a hard ceiling once additive tiers engage. Separately, `coarse-state-near-tie-retention-disabled-retry`'s own additive work pool (`orchestration.ts`) now derives from the solve's resolved `workBudget` (`scaledStageWorkBudget`, `budget-units.ts`) instead of re-deriving a second, independent conversion from `timeBudgetMs` — rigorously proven behavior-preserving for live play (the tier never runs there) and for the plain-default no-override call shape (full 160-level `solver:regression --check`, byte-identical node count both before and after). This is **not** a pure no-op for the offline capability-sweep/confirmation-workflow call shape itself (explicit `workBudget` deliberately disproportionate to a huge non-binding `timeBudgetMs`): the tier's own work pool there shrinks from an effectively-infinite ms-derived number to the caller's real, comparably-sized `workBudget` — a genuine, deliberate dose correction, not a discovered bug in the new code. The full `pos:1-10` population (the participation audit's own reference sample) confirmed this at confirmation scale: 0 solved-set gains/losses (3/10 solved both before and after, identical winning stage on every solve), alongside the expected ~2.5x drop in this tier's own `workSpent` and no meaningful change to any other tier's. [`migration report`](../reports/2026-08-28-dedup-near-tie-retry-work-dose-migration.md) has the full account and reproduction commands; the remaining 8 ms-shaped sites are unmigrated and each need their own site-specific behavior-preservation check before conversion — do not batch-convert. **2026-08-28, same day: `repair-fallback` migrated (second site).** Same pattern: its fresh `withWorkCapScope` cap now derives from `scaledStageWorkBudget(workBudget, repairAdditiveBudgetMultiplier, MIN_ATTEMPT_WORK)` instead of `legacyMsToWork` on a `timeBudgetMs`-derived total; `REPAIR_ADDITIVE_BUDGET_MULTIPLIER` is the integer `6.0`, so this reproduces the exact same number in the default no-override case (full 160-level `solver:regression --check` stayed byte-identical, 68,562,085 nodes). One pre-existing regression test (`the ordinary repair fallback loop gets fresh work room...`) had pinned the OLD ms-derived magnitude (~100.5M, from an explicit `workBudget: 100_000` the old code silently ignored in favor of `timeBudgetMs`) — updated to pin the new, correct value (`600,000` = `6 * 100,000`) with an explanatory comment; this is a genuine test-intent-preserving update, not a weakened assertion (the test still confirms a fresh, non-inherited cap, just a smaller and now workBudget-honoring one). The `pos:1-10` capability-sweep-shape confirmation matched exactly on every field this time (solved set, per-level `nodesExpanded`, and every stage's `workSpent` including `repair-fallback`'s own) — a stronger result than the first site's confirmation, but one that turned out not to exercise the work-cap dimension at all: this tier's node ceiling binds first on this population either way, old cap or new. [`repair-fallback migration report`](../reports/2026-08-28-repair-fallback-work-dose-migration.md) has the full account. **2026-08-28, same day: `admissible-order-alternate-tiebreak-retry` migrated (third site), and a distinct structural gap found in the sibling plain `admissible-order-fallback` tier.** The retry tier follows the identical pattern (integer `1.0` fraction, `withWorkCapScope`-installed fresh cap, byte-identical default-shape `solver:regression --check`, and — third time in a row — an exact-match `pos:1-10` confirmation on every field including its own `workSpent`); see [`migration report`](../reports/2026-08-28-admissible-order-non-default-retry-work-dose-migration.md). While planning it, the plain `admissible-order` tier (immediately before it in the ladder, a distinct tier) was found to install **no fresh work cap at all** under legacy semantics — unlike every migrated-or-migratable site, it has nothing to swap; it silently inherits whatever `prep._workCap` an earlier tier's last attempt left, empirically observed as both fully unbounded (`null`) and an arbitrary small leftover unrelated to `ADMISSIBLE_ORDER_BUDGET_FRACTION`. This is not one of the 9 CI-ratchet-tracked ms-shaped sites (it has no `legacyMsToWork` call to remove) and is architecturally a different, larger question — a genuine new-allocation *addition* (the same class of change as `repair-fallback`'s own 2026-08-20 fix), not a currency swap — so it is **not** being folded into this migration pass. A follow-up full-`pos:1-10` probe found **zero cases of actual work-dimension starvation** on that population — the node-reserve ceiling governs every one of the 10 levels' `admissible-order` attempts regardless of whether the inherited work cap was `null` or a stale finite number, downgrading this from "plausible correctness risk" to "confirmed structural gap, no demonstrated impact on this sample" (status `inconclusive`, not `active`). See [`2026-08-28-admissible-order-work-cap-gap-discovery.md`](../reports/2026-08-28-admissible-order-work-cap-gap-discovery.md) for the full finding and its own next-step gate; zero live-play risk either way (same `disableExtraBudgetPasses` protection as every sibling tier). Five sites remain unmigrated in the original nine-site inventory (`goal-attraction-disabled-retry`, `admissible-order-fallback` itself — pending the gap decision above, `connectivity-axis-prune-disabled-retry`, `repair-elite-prefix-dfs-retry`, `must-cross-neighbor-prune-disabled-retry`, `guidance-goal-distance-retry` — six names, one of which needs the gap resolved first). **2026-08-28 EW1 gate complete:** the first canonical equal-work census pilot is concluded on a deterministic 60-level frozen-gap sample, 34 base techniques, 2,015 mechanically eligible cells, and 10M canonical work per cell. Its first execution exposed and led to a regression-tested fix for an admissible-order/IDA cap-contract hole (`_workCap` vs `_strictWorkCap`); corrected run `33156541827` has complete uncensored coverage and a **12/60 oracle union**. Beams contribute 33 successful cells and solve 8 levels, **6 beam-exclusive**, while IDA solves 3 levels (2 exclusive), ordinary DFS 2 (1 exclusive), and repair 2 (1 exclusive). Every beam self-exhausts below 10M; almost every unsuccessful DFS/IDA/repair cell consumes the cap. This validates the scheduler architecture in the common work currency: cheap beam screens first, preserve distinct deep capability (especially repair), and make DFS/IDA continuations compete for residual work. **Next Workstream 2 gate:** join this corrected equal-work pricing with current production lifecycle reach/work using `solver:analyze-equal-work-production-reach` against an exact-current-head lifecycle-telemetry report, require its `--check` gate to pass, then prespecify a production-shaped static fixed-work repricing experiment; do not expand EW1 merely for smoother rankings. [`additive-tier-participation audit`](../reports/2026-08-28-additive-tier-participation-audit.md), [`session-scope fix`](../reports/2026-08-28-discovery-work-meter-session-scope-fix.md), [`budget rationalization`](../reports/2026-08-27-solver-budget-model-rationalization.md), [`budget contract`](solver-budget-determinism.md), [`scheduler contract`](solver-scheduling-policy.md), [`static repricing join`](../reports/2026-08-25-scheduler-static-repricing-join.md) |
| 6 | Repair reachability/reconstructability | **ACTIVE, SECONDARY** | Allocation/access/seed-continuation questions live upstream under Workstreams 0 and 1. Here, do not repeat the already-classified frozen-prefix cases `R00648`, `R03176`, `R00630`, or `R02449`. **2026-08-27:** `R00630`/`R02449` classified — both solved by `searchCompletionFromPartialPath` (unlike `R00648`/`R03176`, which defeat it), but at opposite cost extremes: `R00630` in 3,247 nodes (under its 4,000-node production budget), `R02449` in 1,268,180 nodes (317x that budget). Four cases now span three regimes (operator-incapable; cheap-reconstruction/retreat-question; reconstructable-but-expensive) with no recurrence in any one — do not build retreat, reconstruction-budget, or destroy machinery from a single case. Large destroy/core-guided work still requires recurrent reconstructability evidence. [`R00630`/`R02449` classification](../reports/2026-08-27-repair-live-prefix-reconstruction-classification-r00630-r02449.md), [`repair audit`](../reports/2026-08-24-repair-reachability-reconstructability-audit.md) |
| 7 | Architectural speed and execution substrate | **ACTIVE SUPPORTING PROGRAM / STATIC SCORER SPECIALIZATION CLOSED NEGATIVE / FUSED PLAIN-CANDIDATE KERNEL CLOSED NEGATIVE** | Fresh current-solver profiling found `beamSearchFromGate` at 30.46% self-time and `scoreMove` at 14.56% on the hard sample, earning the bounded scorer pilot. The conservative plain/default/no-ordering-bias specialization preserved every solve/node trace but was +0.91% slower on published and effectively flat (-0.05%) on hard Corpus-2, so that exact branch-deletion form is closed. **2026-08-27 breakdown:** the existing `PF_BEAM_DEBUG=1` debug-only counters, run on three independent 24-level hard-Corpus-2 stride samples plus the full 160-level published corpus, all agree: candidate generation/apply/undo (excluding the nested connectivity timer) is the dominant disjoint bucket at 46.5-55.3% of instrumented beam time on every workload, versus 16.6-27.2% for connectivity and 12.3-16.2% for path replay, nominating the documented fused-JS move/state kernel per the pre-declared decision rule. **2026-08-27 fused-kernel pilot, closed negative:** a bounded eligibility-gated (no must-pass/must-cross/portal/flipper/landmark mechanics, `cfg==null`, non-research) inline replacement of `applyMove`/`evaluatePrunedMove`/`undoMove` — no `UndoToken` allocation, primitive non-closure undo storage — preserved every solve/node trace (byte-identical on the exact eligible-level sets and on the full 160-level `solver:regression --check`) but was +3.13% slower (geometric mean, slower in all 5 reps) on the 24 published levels that engage it and effectively flat/noisy (-0.11%) on the 4 Corpus-2 levels that do; only 24/160 published and 4/1700 Corpus-2 levels are ever eligible for this exact mechanic-free shape. Close this exact branch-inlining form; the code was developed and measured only in disposable worktrees and was never committed. Replay/state-materialization and connectivity stay closed/untouched by this pilot (measured secondary, not targeted). Native/WASM remains closed. A materially different fused-kernel candidate needs a different mechanism (see the pilot report's "what this does and does not establish"), not a wider eligibility gate on the same approach. [`fused plain-candidate kernel pilot`](../reports/2026-08-27-fused-plain-candidate-kernel-pilot.md), [`beam cost breakdown`](../reports/2026-08-27-beam-cost-breakdown-candidate-generation-dominant.md), [`scorer pilot`](../reports/2026-08-26-current-head-specialized-scorer-pilot.md), [`speed audit`](../reports/2026-08-24-speed-substrate-static-audit.md) |

### 2026-09-01 technique-niche synthesis

The frozen-census/static-feature synthesis does **not** change queue priority. It reinforces Workstream 2's corrected-EW1/current-production `workSpent` join: naturally exhausting beams are cheap screens, repair retains distinct deep capability, and overlapping DFS/IDA continuations should compete only after budget ownership is coherent. The frozen census must not be read as a current-capability oracle: 14 production-solved levels had no frozen T1 winner, so `no frozen T1 winner` and `no current technique` are explicitly different claims. For Workstream 1, continue the deeper offline niche/relative-advantage and residual analysis after the current-head work join; a fixed-work shadow selector over a small operationally distinct menu is a candidate next experiment only if legal static features show actionable headroom while preserving singleton/doubleton recall. Static descriptors currently stratify frozen-T1 no-winner risk but remain too weak for precise technique/cost prediction. No closed workstream is reopened. See [`technique niches and unsupported anatomy`](../reports/2026-09-01-technique-niches-and-unsupported-level-anatomy.md).

## Promoted/completed workstreams

These rows remain in the register because their evidence and IDs are still referenced by current research, but they do not represent standalone work ahead of the immediate execution priority.

| ID | Workstream | State | Next gate |
|---:|---|---|---|
| 3 | Generalization and holdout discipline | **METHOD REVISED / SUPPORTING, NOT A STANDALONE RESEARCH BLOCKER** | The 2026 one-fresh-cohort-per-candidate lifecycle proved the need for independent confirmation but overbuilt the mechanism. Current rule: evidence intensity scales with selection pressure; same-generator fresh/locked blocks provide confirmation; cross-generator transfer means a materially different construction/source process; repeated confirmation should consume pre-partitioned blocks rather than whole pools. Do **not** build a shared pool merely to satisfy the framework. Exercise the revised contract through Workstream 1's topology-composition challenge; if several future selected candidates are queued, then materialize a locked random-generator confirmation pool with deterministic blocks. `transfer-envelope-001` stays untouched but is reclassified as an in-envelope same-generator challenge, not cross-generator transfer. [`evaluation evidence`](solver-evaluation-evidence.md), [`historical cohort record`](../reports/2026-08-24-solver-confirmation-transfer-cohort-reservation.md) |
| 8 | Remaining cheap isolated capability missed by production | **SUBSUMED BY WORKSTREAM 1; MEASURED** | Keep isolated winners as action-selection evidence. Do not append a permanent tail solely because an isolated winner exists. [`post-976 rejoin`](../reports/2026-08-25-post-976-portfolio-exposure-rejoin.md) |

## Closed negative workstreams

These tested forms are closed. Reopening requires materially new evidence or a materially different premise, as stated in each row.

| ID | Workstream | State | Next gate |
|---:|---|---|---|
| 0 | Restart/randomization and learned-failure search | **RESTART: PRE-WIRING PILOT AT W=150,000,000 CAME BACK A CLEAN NULL; PRODUCTION WIRING CLOSED FOR NOW. LEARNED-FAILURE: STAGE A POSITIVE, STAGE B NEGATIVE — CROSS-LEVEL CONNECTIVITY CERTIFICATE DIRECTION CLOSED** | At `W=16,000,000` on the 20-level near-miss residual stratum (census-unsolved, `bestBadness<=6`), continuation and restart were statistically indistinguishable (0/20 solved everywhere). A materially larger `W=64,000,000` (4x, same stratum, same 0.5 split) reversed that: continuation 1/20, restart 3/20, 2 clean gains, 0 losses. An independent confirmation on the disjoint remaining 23 levels of the same 43-level stratum (identical frozen design, no new independent variable) **replicated and strengthened** the signal: continuation 2/23, restart 5/23, 3 more clean gains, 0 losses. **Pooled across all 43 stratum levels: continuation 3/43 (7.0%), restart 8/43 (18.6%), 5 clean restart-only gains, 0 losses in either draw.** **2026-08-27 production candidate design:** worked out that production's real shape is an 8-way additive seed fan-out (not continuation, not the tested 2-way split) with an effective ~140-160,000,000 work-unit worst-case ceiling (repair spends ~4x canonical work per node vs. plain DFS/beam) — designed `STRATEGY_REPAIR_LATE_PROBE_RESTART_SPLIT` to replace that 8-seed reserve with a 2-way 50/50 split of the same total ceiling, gated on one prespecified pre-wiring pilot at `W=150,000,000`. **2026-08-27, same day, pilot result: clean null.** Full 36-level `bestBadness` 7-9 population (the `<=6` stratum is fully spent), `W=150,000,000`: continuation solved 9/36, restart solved 9/36, **0 restart-only gains, 0 restart-only losses** — an exact tie on the identical solved set, not a shrunken effect. The restart advantage does **not** monotonically grow with `W`: absent at 16M, clear at 64M, absent again at 150M (on a different, harder badness band, since the `<=6` band was already spent — the pilot cannot cleanly separate "effect vanishes at large `W`" from "effect is specific to the near-miss band"). **Close `STRATEGY_REPAIR_LATE_PROBE_RESTART_SPLIT` as designed; do not implement it.** If restart-vs-continuation is revisited, the open question is the effect's shape vs. `W`/population band, not this wiring design. **2026-08-28:** Learned-failure search's Stage A (rejection-population audit, see `learned-failure audit`) is now implemented and has run once: an 80-level/67,179-rejection unselected corpus2 sample was **not** a Stage-A negative — coarse-context recurrence survives across genuinely different exact states and different levels (55.9% overall; 11.3% for the dominant, index-free `goal`-unreachable subtype, whose single most common shape spans 29 distinct levels). This earns a Stage B structural-reason-sketch pilot; it does not yet establish a sound learned reason or justify any cache. Instrumentation (`modules/solver/topology.ts`'s `ConnectivityRejectionObserver`) verified byte-identical against `solver:regression --check`. **2026-08-28, same day, Stage B result:** the boundary/blocker sketch (`_computeBoundarySketch`, gated behind `includeBoundarySketch`, also verified byte-identical) ran on the same 80-level sample's dominant `goal`-unreachable cluster (12,905 records, 7,934 distinct exact states). Recurrence of the reached-shape materially exceeded exact-state recurrence (83.1% of records share a shape with another record, vs. 52.6% sharing the literal exact state) — but 91.2% of that recurrence is confined to a single level (only 1.9% of distinct shapes, 8.8% of records, ever span more than one level), tripping the certificate-audit's own stop condition ("recurrence is mostly one parent/family"). **Close the cross-level connectivity-derived structural certificate direction; do not build a bounded reason checker from it.** A within-solve (single-level) boundary-shape memo is a distinct, narrower candidate the Stage B report surfaces but does not scope, soundness-test, or recommend. [`Stage A audit`](../reports/2026-08-28-connectivity-rejection-stage-a-audit.md), [`Stage B audit`](../reports/2026-08-28-connectivity-rejection-stage-b-audit.md), [`W=150M pre-wiring pilot (null)`](../reports/2026-08-27-repair-restart-continuation-w150m-pre-wiring-pilot-null.md), [`production candidate design`](../reports/2026-08-27-repair-restart-continuation-production-candidate-design.md), [`confirmation pilot`](../reports/2026-08-26-restart-continuation-larger-w-confirmation.md), [`larger-W pilot`](../reports/2026-08-26-restart-continuation-larger-w-pilot.md), [`restart audit`](../reports/2026-08-24-restart-continuation-value-audit.md), [`execution-readiness harness`](../reports/2026-08-26-restart-continuation-execution-readiness.md), [`corrected near-miss pilot`](../reports/2026-08-26-restart-vs-continuation-near-miss-development-pilot-corrected.md) (supersedes the original, metric-bugged [pilot](../reports/2026-08-26-restart-vs-continuation-near-miss-development-pilot.md)), [`learned-failure audit`](../reports/2026-08-24-learned-failure-certificate-audit.md) |
| 4 | Beam score/retention at proven extinction boundaries | **SCOPED QUOTA GATE NEGATIVE / NO INTERVENTION** | Keep the tested fixed-width quota/bucketing form closed. The 976 gains support routing existing beam identities, not new survivor-selection machinery. Reopen only with materially new independent evidence for a bounded retention mechanism. [`projection`](../reports/2026-08-25-beam-full-pool-survivor-projection.md) |

## Deferred workstreams

These are on-demand or blocked workstreams rather than current execution priorities.

| ID | Workstream | State | Next gate |
|---:|---|---|---|
| 5 | Exact/reference-model program | **BOUNDED INFRASTRUCTURE / MATRIX CLOSED** | Use CP-SAT/reference work only for a concrete prioritized label, counterexample, or certificate. Do not expand validation generically. [`reference-model audit`](../reports/2026-08-23-solver-reference-model-capability-audit.md) |

## Closed evidence blocker: alleged cross-stage admissible dependence

The former P0 is retired. Immutable run `32459711208` shows the eight rows previously attributed to `admissible-order` were actually later mechanic-bucket-retention retry wins; the lifecycle reducer had a stale hard-coded stage list. Do not resume MP/MC memo archaeology from this evidence. If a future same-action, same-resource fresh-versus-preceded discrepancy appears, use the paired deterministic trace contract first.

## Cross-item guardrails

- **Scheduler:** global suppression of ordinary main-search `dfs|score=objectiveFirst|bias=none` + `dfs|score=intersectionHarvest|bias=none` is closed after independent confirmation lost two solves.
- **Action selection:** selective mechanic-bucket-retention IH exposure for the two very-high-intersection bundles is closed as a general promotion candidate after a null fresh confirmation.
- **Generalization:** spent confirmation rows remain development evidence for descendants. `transfer-envelope-001` is still pristine but is a same-generator in-envelope challenge, not cross-generator transfer. Use untouched blocks rather than disposable whole corpora for repeated confirmation; use topology-composition or another materially different source for distributional transfer claims.
- **Beam:** routing/exposure evidence does not reopen the negative quota/bucketing result or justify universal widening.
- **Restarts:** additive multi-seed gains are not proof that restart beats continuation at equal work. Now measured on a near-miss residual stratum, both a 50/50 and an 0.8/0.2 split: a tie against continuation on both solved count (0/20 both) and best-badness (17-19/20 exact ties) — no promotion case for restart, but not a continuation win either. See the 2026-08-26 corrected development pilot (the first pass had a best-badness aggregation bug, since fixed).
- **Learned failure:** generic CDCL/LCG/nogood infrastructure remains unjustified without a recurring cheap sound reason family.
- **Repair:** exact-live can still be reconstruction-hard; retreat depth and reconstruction ability are separate questions.
- **Speed:** implementation speed and search policy remain separate.

## Common closed forms

Do not reopen unchanged merely because code/reports survive: the independently rejected global two-DFS suppression; the selectively exposed diverse-IH rule tested in `32911007113`/`32912881453`; the must-cross-heavy reserve-widen + must-pass-heavy/default sibling-rule plain-beam exposure tested in `32946074849`/`32946077662` (`STRATEGY_MUSTCROSS_RESERVE_WIDEN_BEAM_EXPOSURE`); generic repair-budget increases; coarse repair-gate widening; universal beam-width increases; the tested low-cardinality beam quota/bucketing keys; broad novelty/MAP-Elites/DPP machinery before a cheap descriptor earns it; generic DFS/beam transposition caching; broad CDCL/LCG/MUS machinery; ordinary “less resource used is better” dominance for exact targets; symmetry retries as a substitute for diagnosing representation bias; dynamic scheduler machinery before simpler fixed-work evidence earns it; bulk variant generation without a specific unanswered question; the designed-but-unimplemented `STRATEGY_REPAIR_LATE_PROBE_RESTART_SPLIT` (2-way 50/50 restart split replacing the production 8-way seed fan-out) at `W=150,000,000` on a `bestBadness` 7-9 population — clean null, 0 gains/0 losses.

Use [`solver-future-work.md`](solver-future-work.md), [`solver-opt-in-experiment-ledger.md`](solver-opt-in-experiment-ledger.md), and dated reports for dispositions and reopen conditions.
