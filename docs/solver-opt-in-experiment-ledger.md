# Solver opt-in experiment disposition ledger

Authoritative compact disposition for solver mechanisms whose **production default is OFF** but code remains invokable. It answers: **is a promotion decision still open?**

It is not a queue. Rank lives in [`solver-optimization-workstreams.md`](solver-optimization-workstreams.md); research/promotion rules in [`solver-research-operating-model.md`](solver-research-operating-model.md); default polarity in `OPT_IN_FEATURES` in [`../modules/solver/ablation-config.ts`](../modules/solver/ablation-config.ts). Documentation checks require every current member of that set below.

Full pre-consolidation chronology: [`archive/snapshots/solver-opt-in-experiment-ledger-2026-08-20.md`](archive/snapshots/solver-opt-in-experiment-ledger-2026-08-20.md). Later measurements remain in dated reports/git history.

## Retention rule

Git is the archive. Closed default-OFF code should remain only when it provides reusable generic plumbing, an unusually valuable counterfactual/diagnostic, or is cheaper/safer to retain than remove. “Could be useful someday,” code presence, and historical interest are not retention reasons.

Likewise, default-ON retries/reserves have no permanent budget entitlement. Scheduler work may reprice, shrink, reorder, condition, decompose, or remove them under a fixed aggregate `workSpent` envelope.

## Current production-default-OFF flags

| Flag | Disposition / reopen condition |
|---|---|
| `PRUNE_PORTAL_PARITY_ENVELOPE` | **CLOSED NEGLIGIBLE.** Sound but effectively non-firing in measured portal populations. Reopen only for a materially stronger formulation. |
| `STRATEGY_PORTAL_COARSE_STATE_MERGE` | **PROMOTION CLOSED NEGATIVE; salvage line also CLOSED NEGATIVE.** Enables beam coarse-state merge on portal-bearing levels (`search.ts`), unconditionally disabled there otherwise. The merge key folds in each candidate's used-portal-pair identity (`BeamNode.usedPortalPairs`) so it cannot alias candidates that consumed different portal pairs. **2026-09-09 frozen matched-work A/B** (954-level portal Corpus-2 population): 158 gains / 12 losses, net +146, all 158 gains referee-valid, but `R01273` is a genuine control-capability regression. **Root cause (exact deterministic repro, 2026-09-11):** two candidates reach the identical coarse key (current cell + all 7 constraint scalars) via different multi-cell corridors; the coarse key's blind spot is trailing visited-cell identity. **Salvage attempted and closed:** a bounded second-survivor retention slot (keyed on distinct predecessor cell) and a predecessor-identity subkey extension (1/2/4-hop lookback) were each tried against the exact deterministic `R01273` reproduction. Each measurably delayed the failure (true death depth 17 -> 20 -> 40 -> 41 -> 43) but none closed it, and the discriminating trailing distance kept growing with no sign of convergence — the report's own prespecified closing condition ("if every safe discriminator effectively disables merging, close the salvage line"). No code from either salvage attempt is retained; the frozen 12-loss/158-gain validation ladder was never reached because the cheapest single-level gate did not close. See [`preflight`](../reports/2026-09-09-portal-coarse-state-merge-ab-001-preflight.md), [`source diagnosis`](../reports/2026-09-10-portal-coarse-state-salvage-source-diagnosis-001.md), and [`R01273 collision forensic`](../reports/2026-09-11-portal-coarse-state-merge-r01273-collision-forensic-001.md). Reopen only with a materially different mechanism (e.g. an untested portal-scoped near-tie-margin widening, or an independently reproduced fix to the near-tie retention capacity limitation noted in the forensic's "side observation"), not a retry of the same subkey/retention-slot shapes. |
| `STRATEGY_REPAIR_ELITE_PREFIX_DFS` | **CLOSED NEGATIVE IN CURRENT FORM.** Dedicated equal-budget test lost against control through ordinary-repair displacement. Descendant needs a cheaper/materially different operator or selector. |
| `STRATEGY_REPAIR_TURN_BIAS` | **CLOSED NEGATIVE.** Deterministic Corpus-2 evidence reproduced a net loss. |
| `STRATEGY_REPAIR_FALLBACK_GATE_WIDEN` | **CLOSED NEGATIVE.** Population-scale broad `isHighInt`/multi-portal widening produced 0 gains / 2 losses. Reopen only with a materially different narrower selection mechanism. |
| `SCORE_GOAL_ATTRACTION_GUIDANCE_DISTANCE` | **CLOSED NEGATIVE, GLOBAL-SWAP FORM.** Selected populations produced gains but larger losses; the useful descendant is the separately promoted dead-last retry. Do not reopen the global form without a selector that avoids the loss population. |
| `STRATEGY_REPAIR_FALLBACK_NODE_RESERVE` | **CLOSED, SAFE BUT USELESS FOR TARGET.** More fallback participation produced no solves; more of the same repair work is not the missing capability. |
| `STRATEGY_ADMISSIBLE_ORDER_PROFILE_NODE_RESERVE` | **CLOSED NEGATIVE, POPULATION-VALIDATED.** Corpus-2 validation lost four solves with essentially flat work. |
| `STRATEGY_REPAIR_BEAM_SEED` | **CLOSED.** Isolated apparent gain vanished through the real full ladder while adding cost. |
| `STRATEGY_REPAIR_SHRINK_RECOVERY` | **RETAINED, NO CURRENT PROMOTION GATE.** Can restore budget withheld by adaptive probe shrink; keep as a counterfactual unless current evidence shows a meaningful shrink-caused regression it fixes economically. |
| `STRATEGY_REPAIR_ELITE_PREFIX_DFS_RETRY` | **CLOSED.** Additive dead-last form removed displacement but recovered zero levels at both tested budgets. |
| `STRATEGY_RETRY_TIER_NODE_STAIRCASE` | **CLOSED NEGATIVE.** Fixed first-config monopolization but traded away load-bearing early work and raised wall cost. Reopen only with a different protection mechanism. |
| `STRATEGY_MUSTCROSS_RESERVE_WIDEN_BEAM_EXPOSURE` | **CLOSED NEGATIVE (2026-08-26).** Widens `MAIN_LOOP_LATE_RESERVE_CONFIG_COUNT` by one (mirroring the validated 4->5 precedent) and appends the missing plain WIDE beam to attempts.ts's other two must-cross-heavy sibling rules (the ones `STRATEGY_MUSTCROSS_FLIPPER_WIDE_BEAM_EXPOSURE` deliberately left untouched because their reserve windows were full). Population-scale development A/B (`solver-routing-regime-sample-ab.yml`, 486-level must-cross-heavy sample): control 389/486, treatment 389/486, 0 gains, 0 losses, with real nonzero work/node engagement. Reopen only with a materially different technique/envelope, or evidence the reserve-widen itself was the limiting factor. See [`2026-08-26-mustcross-reserve-widen-beam-exposure-development-ab.md`](../reports/2026-08-26-mustcross-reserve-widen-beam-exposure-development-ab.md). |
| `STRATEGY_HIGHINT_STANDARD_INTERSECTION_HARVEST_BEAM_EXPOSURE` | **CLOSED NEGATIVE (2026-08-28, APPEND-LAST FORM).** Selected replay was +1/-0, but the prespecified mechanics-eligible 120-level strict-67M development A/B was control 56/120 vs treatment 55/120: 0 gains, 1 loss (`R02965`), new action reached 68/120, +29.0M aggregate work, no errors/deadline censoring. Appending the new beam changed the fixed five-config protected suffix and starved an existing `beam|score=objectiveFirst|bias=none|width=5000|retention=plain` winner. Do not repeat append-last placement. See [development report](../reports/2026-08-28-highint-standard-ih-exposure-development.md). |
| `STRATEGY_ADMISSIBLE_ORDER_NON_DEFAULT_RETRY_WORK_CAP_ENFORCEMENT` | **NEW (2026-09-10), untested architecture prerequisite, not itself a promotion candidate.** Makes `admissibleOrderSearch`'s hot loop additionally consult `prep._workCap`, but only for the admissible-order-non-default-retry tier's dispatch. It closes the architectural gap found by [`2026-09-10-admissible-order-non-default-retry-matched-work-methodology-001.md`](../reports/2026-09-10-admissible-order-non-default-retry-matched-work-methodology-001.md): without this, the queued `1.0 -> 0.18` fraction could not actually bind. Unit tests cover default-off no-op behavior, enabled cap enforcement, and isolation from the sibling fallback tier. It carries **zero population-scale evidence**. A matched-work confirmation is now technically meaningful, but current rank is conditional on the post-1,029 residual atlas; see the live workstream authority. |
| `STRATEGY_HIGHINT_STANDARD_INTERSECTION_HARVEST_RESERVE_PRESERVING_EXPOSURE` | **CLOSED NEGATIVE (2026-08-28).** Same missing `beam|score=intersectionHarvest|bias=none|width=2000|retention=plain` and same very-high-intersection rules as the append-last parent, but inserted immediately before the old protected five-config suffix. Selected mechanism replay run `33151801662` was still +1/-1: `R02440` remained a direct treatment rescue, but `R02965` still regressed. Preserving suffix membership did not preserve the old winner's usable allocation because the inserted beam consumed work before it; the 5K objective winner stopped about 134K work short of its known solve. Gate 2's disjoint 120-level cohort was therefore never executed. Do not repeat nearby placement tweaks without a materially new scheduler premise. See [development report](../reports/2026-08-28-highint-standard-ih-reserve-preserving-development.md). |
| `PRUNE_MC_PORTAL_FORCED_NEIGHBOR` | **NEW (2026-09-11), unpromoted.** Prunes a pending must-cross cell's still-open axis once its forced neighbor is a visited portal terminal — categorically unenterable again per `search-state.ts`'s portal-revisit rule, a case `PRUNE_MC_FORCED_NEIGHBOR`/`PRUNE_MC_NEIGHBOR_BUDGET` do not close (budget-independent, unlike the latter). Observer-only pilot cleared all four promotion gates: 0 false rejects across the oracle-labelled atlas, a full 3-corpus known-solution replay, and a class-4/class-5 real-search run; 15 unique oracle-atlas dead-branch catches beyond the existing gauntlet. See [`observer pilot`](../reports/2026-09-11-joint-obligation-propagation-observer-pilot-001.md). Carries **zero population-scale solve-count evidence** — a frozen matched-work A/B is the next gate before promotion. |

## Recently promoted/default-ON mechanisms worth remembering

Listed only so old experiment names are not mistaken for dangling opt-in tasks. Code owns polarity; [`solver-scheduling-policy.md`](solver-scheduling-policy.md) owns future budget valuation.

| Mechanism | Current disposition |
|---|---|
| `PRUNE_MC_NEIGHBOR_BUDGET` | Default-ON after level-blind population gain; retry-tier evidence shows it can still alter search order in both directions. |
| `PRUNE_MC_NEIGHBOR_BUDGET_PORTAL` | Default-ON, promoted 2026-09-09 after the portal derivation closed and the frozen matched-work A/B on the 530-level portal+must-cross Corpus-2 population found 52 gains / 0 losses, every gain referee-valid, with zero published-corpus regressions. See [`preflight`](../reports/2026-09-09-mc-neighbor-budget-portal-ab-001-preflight.md). |
| `PRUNE_CONNECTIVITY_VOLUME_PORTAL` | Default-ON, promoted 2026-09-10 after the portal derivation closed and known-solution soundness checks passed. Frozen matched-work A/B on 954 portal Corpus-2 levels: **2 gains (`R02297`, `R02746`) / 0 losses**, plus a small aggregate-work reduction. Does not cover `isConnectedForFalseGoalTriggerSearch`. See [`preflight`](../reports/2026-09-09-connectivity-volume-portal-ab-001-preflight.md). |
| `STRATEGY_GOAL_ATTRACTION_DISABLED_RETRY_NODE_RESERVE` + `STRATEGY_GOAL_ATTRACTION_DISABLED_RETRY_FRESH_WORK_POOL` | Both default-ON, promoted together 2026-09-10 as a validated pair. Confirmation-002 on a reach-conditioned, disjoint 150-level Corpus-2 population: control 14/150 vs treatment 17/150, **+3/-0** (`R01124`, `R02020`, `R02060`), all directly attributable to the retry stage. Tier reach rose 104/150 to 140/150 with real work engagement and zero errors/truncation. See [`confirmation`](../reports/2026-09-05-goal-attraction-disabled-retry-fresh-work-pool-confirmation-002-preflight.md). |
| `STRATEGY_COARSE_STATE_NEAR_TIE_RETENTION_RETRY` | Default-ON additive dead-last retry; safe placement does not make its work free. |
| `STRATEGY_ADMISSIBLE_ORDER_NON_DEFAULT_RETRY` | Default-ON after population gain; retain as baseline but reprice residual value. Confirmation 006 found fraction `0.18` vs `1.0` lost zero solves on the 76-level informative population. A later fresh 150-level production A/B was non-informative because the target stage recorded attempts on 69 levels while expanding **0 nodes**, the strict whole-solve cap having been exhausted upstream. Production remains `1.0`; any next test must enforce nonzero target-stage-work participation under a matched aggregate envelope. The newly added tier-scoped enforcement path now makes such a test technically possible. See [`production A/B`](../reports/2026-09-05-admissible-order-non-default-retry-production-ab-001.md). |
| `STRATEGY_CONNECTIVITY_AXIS_EXHAUSTED_RETRY` | Default-ON after population gain; retain as baseline but reprice residual value. Its high DFS work cost reproduces the first-config-monopolization mechanism whose attempted staircase fix was already closed negative, while the tier still carries a small genuinely unique production contribution. See `reports/2026-09-04-whole-ladder-retry-tier-dfs-monopolization-forensic-note-001.md` and `reports/2026-09-04-must-cross-connectivity-axis-production-win-redundancy-001.md`. |
| `STRATEGY_MC_NEIGHBOR_BUDGET_RETRY` | Default-ON after +9 / 0-loss Corpus-2 evidence; material cost makes it a scheduler tail-audit candidate, but its per-config staircase is already part of the mechanism and carries a small genuinely unique production contribution. See `reports/2026-09-04-whole-ladder-retry-tier-dfs-monopolization-forensic-note-001.md` and `reports/2026-09-04-must-cross-connectivity-axis-production-win-redundancy-001.md`. |
| `STRATEGY_GOAL_ATTRACTION_GUIDANCE_DISTANCE_RETRY` | Default-ON dead-last descendant of the negative global swap; population test added three solves without losses in reached controls. Reprice through scheduler work rather than increasing reserve in isolation. |
| `STRATEGY_REPAIR_LATE_PROBE_MULTI_SEED_RETRY` | Default-ON after five additive rescues without measured losses. **Seven-to-six seed shrink CLOSED NEGATIVE (2026-09-10):** a reach-conditioned population-scale confirmation found seed 7 uniquely rescues `R02460` and `R02553`; keep all seven seeds. The modest work saving from truncation does not clear specialist retention. Do not retry the unchanged shrink absent materially new evidence. See [`result`](../reports/2026-09-10-repair-late-probe-six-seed-confirmation-001-result.md). |
| `STRATEGY_REPAIR_LATE_PROBE` | Default-ON after same-commit population gain; future cap growth competes with other actions under shared work. |
| `STRATEGY_MAIN_SEARCH_LATE_RESERVE` | Historical default-ON mechanism. Its broad “give late repair more work” interpretation is closed; do not infer an active research lane from code presence. |
| `STRATEGY_MUSTCROSS_FLIPPER_WIDE_BEAM_EXPOSURE` | Default-ON, promoted 2026-08-27 after development A/B +3/-0, same-generator confirmation +3/-0 (`confirm-residual-003`), and a cross-generator topology-composition transfer attempt that returned a clean null with zero losses. Appends plain wide intersectionHarvest/objectiveFirst beams to the must-cross+flipper-heavy rule only; sibling must-cross-heavy rules remain untouched. See [`2026-08-24-solver-confirmation-transfer-cohort-reservation.md`](../reports/2026-08-24-solver-confirmation-transfer-cohort-reservation.md). |

This is intentionally not an exhaustive production-feature list.

## Interpretation rules

1. Default-OFF does not mean pending; most retained opt-ins are closed.
2. Code presence does not reopen a result.
3. Evidence applies to the tested wiring/protocol; a materially different mechanism may need a new verdict.
4. Historical hints/winners/level outcomes may label research but may not steer cold level-blind capability.
5. A small decisive negative can close a form; do not buy a giant run merely for ceremony.
6. Isolated-technique wins nominate full-policy tests; they do not promote themselves.
7. A polarity change must update both `OPT_IN_FEATURES` and read-site semantics.
8. Compare gains, losses, `workSpent`, and wall cost; no one metric is sufficient.
9. Dead-last placement proves non-interference with earlier winners, not economic value.