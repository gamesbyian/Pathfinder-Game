# Naming-cleanup Phase 8 batch 8D execution record

## 0. Execution identity

| Field | Value |
| --- | --- |
| Phase | 8 |
| Batch | 8D |
| Status | closeout |
| Base `main` SHA | `e6a385a554ff176c90265f479559562afd9852fd` (merge commit of batch 8C's PR #1588) |
| Branch | `claude/naming-cleanup-plan-nzv395` (restarted from new `main` after batch 8C merged) |
| PR | pending (not yet opened) |
| Selected ledger row IDs | NC-P08-021, NC-P08-022, NC-P08-042 |
| Reconciliation mode | delta (single-batch implementation session immediately following batch 8C's merge; no unrelated Phase 8-14 architecture landed on `main` in between) |
| Highest risk in batch | medium (all 3 rows) |
| Primary compatibility owner | none (no dual-read row in this batch) |
| Canonical mappings | see Section 5.6 (technique-census second-order analysis) of `naming-cleanup-plan.md` |
| Implementation agent/session | Claude Code remote session (this session) |
| Closeout auditor | same session (no fresh-session closeout was available; same limitation noted in batches 8A-8C) |

### Branch/PR authority preflight

- [x] searched open naming-cleanup PRs and similarly named branches — only this branch and `main` exist; PRs #1586-#1588 (8A-8C) are merged and closed, no other open naming-cleanup PR;
- [x] compared this branch against current `main` after the restart — `git checkout -B claude/naming-cleanup-plan-nzv395 origin/main` puts the branch exactly at `main`'s tip (`e6a385a5...`), plus one small commit recording the 8C merge barrier before this batch's implementation began;
- [x] recovered/superseded relevant commits — none needed;
- [x] confirmed this is the only active implementation batch — ledger `activeExecution.status` was `idle` with `batch: null` before this session started batch 8D, and `batchCompletions["8C"].status` was `merged` (PR #1588, commit `e6a385a5...`) before batch 8D's rows were touched;
- [x] confirmed the branch starts from the recorded current-`main` SHA above.

| Branch / PR | Unique relevant work? | Disposition |
| --- | --- | --- |
| `claude/naming-cleanup-plan-nzv395` (post-8C-merge state) | yes — this batch's implementation | active batch branch |

## 1. Scope, change envelope, and stop conditions

Batch 8D covers technique-census analysis: the second-order re-analysis tool/doc pair, and the equal-work census analyzer (Section 5.6 of `naming-cleanup-plan.md`), per `docs/naming-cleanup-phase-records/phase-08.md`'s 8D scope row. Smallest batch so far (3 rows).

### Change envelope

**Intended observable deltas**

- `scripts/technique-census-second-order.mjs` -> `scripts/analyze-technique-census.mjs` (+ its node-test, renamed to `scripts/analyze-technique-census-node-test.mjs`); the tool's own self-referential "Generated by `scripts/...`" string inside its generated-markdown-builder function updated to the new filename; `test:technique-census-second-order` -> `test:analyze-technique-census` (including the `test:node` aggregate string) (NC-P08-021).
- `docs/technique-census-second-order-analysis.md` -> `docs/technique-census-analysis.md`; its own title updated from "Technique census: second-order analysis" to "Technique census analysis"; its rebuild-command example updated to the new tool filename; every cross-reference link in `docs/README.md`, `docs/tooling-catalog.md` (2 mentions), `docs/solver-correctness-hardening.md`, `docs/testing.md`, `docs/solver-scheduling-policy.md` (2 mentions via one line), and `docs/solver-technique-operational-taxonomy.md` updated to the new filename/title (NC-P08-022).
- `scripts/stress/analyze-equal-work-census-pilot.mjs` -> `scripts/stress/analyze-equal-work-census.mjs` (no internal self-references existed to fix); `scripts/naming-cleanup-phase8-cli-smoke-node-test.mjs`'s fast-fail check target and `docs/tooling-catalog.md`'s one reference updated (NC-P08-042).

**Invariant observables**

- `analyzeTechniqueCensus`, `binaryMutualInformation`, `exactDiscordancePValue`, `renderTechniqueCensusSecondOrder`, and `validateTechniqueBudgetCurves` (all internal function names, none renamed by this batch) are unchanged — proven by the unchanged node-test suite and a real full analysis run against the committed census data;
- `analyze-equal-work-census.mjs`'s pricing/depth-join and greedy-cover logic is unchanged (pure file rename, no content edit was even needed);
- the checked-in generated evidence `reports/stress/technique-census/32240161854/second-order-analysis.json` and `.md`, and every other file under that directory, are byte-for-byte unchanged;
- no solver search behavior, work allocation, or corpus content changed anywhere in this batch.

**Out of scope / separate authorization**

- the generated report's own title text ("Technique census: second-order existing-data analysis", emitted by `renderTechniqueCensusSecondOrder`) and the default output filenames `second-order-analysis.json`/`second-order-analysis.md` were deliberately **not** renamed. These are a genuinely persisted generated-report identity: `scripts/technique-operational-similarity.mjs` reads `reports/stress/technique-census/32240161854/second-order-analysis.json` as its hardcoded default `--source`, and `docs/technique-census-analysis.md` itself documents `second-order-analysis.json.techniqueBudgetCurves` as the scheduler-facing interface. Renaming either the generated title or the default output filenames would be a separate Section 3.2 persisted-identity migration (producer/reader/historical-fixture inventory) not authorized by NC-P08-021/022, and would require updating `technique-operational-similarity.mjs`'s default path and reconciling every already-committed run directory under `reports/stress/technique-census/`. The internal function name `renderTechniqueCensusSecondOrder` is left unchanged for the same reason — it is the code identity behind that retained generated-report title, and "second-order" here is genuine methodological vocabulary (a re-analysis of already-collected census data), not a banned lifecycle label like "pilot".
- `analyze-technique-census.mjs --check` reports the two `reports/stress/technique-census/32240161854/` files as stale against a fresh re-derivation. This is a **pre-existing** condition, confirmed identical (same two filenames, same error) when run against the unmodified pre-batch code via `git stash` — not something this batch's rename introduced. Fixing it would mean regenerating/recommitting evidence data, which is explicitly out of scope for a behavior-preserving naming batch (Section 1's non-goals: "Do not change ... corpus contents, or evidence disposition"). Recorded here for visibility, not as a blocker.

No behavior/resource-policy change, ambiguous historical identity, unowned compatibility boundary, unidentifiable live consumer, or superseding architecture was discovered. No stop condition was triggered.

## 2. Pre-edit impact map

Commands run:

```sh
npm run naming:status
find . -iname "technique-census-second-order*" -o -iname "analyze-equal-work-census-pilot*" (excluding node_modules/.git/.solver-tools/.cache/coverage)
grep -rln <old-term> . (excluding node_modules/.git/reports/docs-archive/data/.solver-tools/.cache/coverage, per term)
```

`npm run naming:status` confirmed Phase 8 gate `ready`, `activeExecution.status: idle`, `batchCompletions["8C"].status: merged`, and batch 8D next with all 3 rows `verification.surfaceInventory: done` already and every other verification dimension `pending`.

Before renaming the tool file, grepped for every reader of `second-order-analysis.json`/`.md` (found `scripts/technique-operational-similarity.mjs` as a real consumer with a hardcoded default path) and confirmed the checked-in `reports/stress/technique-census/32240161854/` directory contains those exact files — this is what drove the Out-of-scope decision above, made *before* editing rather than discovered as a surprise afterward.

### Target occupancy / collision check

| Canonical target | Existing live use? | Same concept / unrelated / collision / already migrated | Disposition |
| --- | --- | --- | --- |
| `scripts/analyze-technique-census.mjs` (+ node-test) | no | n/a | clear |
| `docs/technique-census-analysis.md` | no | n/a | clear |
| `scripts/stress/analyze-equal-work-census.mjs` | no | n/a | clear |

No canonical target was occupied by a materially different live concept.

### Contract-migration matrix

| Surface | Classification | Concrete locations | Evidence / planned test |
| --- | --- | --- | --- |
| Definition / producer | migrate | `technique-census-second-order.mjs`, its node-test, `technique-census-second-order-analysis.md`, `analyze-equal-work-census-pilot.mjs` | `git mv` + content edits, see Section 6 |
| Internal direct consumers | migrate | `docs/README.md`, `docs/tooling-catalog.md`, `docs/solver-correctness-hardening.md`, `docs/testing.md`, `docs/solver-scheduling-policy.md`, `docs/solver-technique-operational-taxonomy.md`, `scripts/naming-cleanup-phase8-cli-smoke-node-test.mjs` | grep-verified zero residue outside frozen/archive/authority-docs/deliberately-retained generated-report identity (Section 10) |
| Canonical parser / normalizer | not applicable | none of these rows touch a persisted-identity parser | n/a |
| Sequential transport | not applicable | none | n/a |
| Alternate worker/race transport | not applicable | none | n/a |
| Serialized writer | retained/frozen (explicit exclusion) | `second-order-analysis.json`/`.md` default output filenames and generated title text | deliberately unrenamed, see Section 1 |
| Historical reader / fixture | retained/frozen | `reports/stress/technique-census/32240161854/*` (all files), dated `reports/*.md` mentioning old names | left unchanged, confirmed by reconciliation grep; `scripts/technique-operational-similarity.mjs`'s default `--source` path continues to resolve correctly (unchanged) |
| Report/export projection | not applicable | none of this batch's rows touch a maintained cross-tool generated-report schema | n/a |
| Analyzer/grouping consumers | not applicable | none | n/a |
| CLI / package alias | migrate | `package.json`'s `test:technique-census-second-order` -> `test:analyze-technique-census`, plus the `test:node` aggregate string; no alias existed for `analyze-equal-work-census-pilot.mjs` (consistent with the plan's own note that it was "now surfaced durable tooling" without ever having gained a package alias) | renamed, no compatibility window needed (not a dual-read row) |
| Workflow command/inputs/outputs | not applicable | no workflow YAML invokes either tool | n/a |
| Artifact/concurrency/cache/path identifiers | not applicable | none | n/a |
| Hint/provenance storage | not applicable | none | n/a |
| Application/UI/editor consumer | not applicable | none of these tools are UI-consumed | n/a |
| Current docs/examples | migrate | 6 docs files, see Internal direct consumers row above | grep-verified (Section 10) |
| Frozen historical evidence | retained/frozen | `reports/stress/technique-census/32240161854/**`, other dated `reports/*.md`, `docs/archive/**` | unchanged; confirmed no mass rewrite |

## 3. Validation topology

| Surface | Real runtime/path | Existing coverage | Coverage class | Gap/action |
| --- | --- | --- | --- | --- |
| `analyze-technique-census.mjs` (library functions) | native Vitest-adjacent Node test | `analyze-technique-census-node-test.mjs` (real synthetic-fixture execution) | direct | ran directly in this session (Section 7) |
| `analyze-technique-census.mjs` (`--check` / full-run CLI) | native Node against real committed data | `test:analyze-technique-census` exercises the library functions only, not the CLI/`--check` path against real data | indirect (library-only) before this session | ran a **real full analysis** (`node scripts/analyze-technique-census.mjs reports/stress/technique-census/32240161854 <tmp-out>`) and a **real `--check`** invocation in this session, closing the gap (Section 7) |
| `analyze-equal-work-census.mjs` | native/bundled Node via `run-bundled.mjs` | `naming-cleanup-phase8-cli-smoke-node-test.mjs` (fast-fail argument check) | ci-test-reference (fast-fail) | ran directly in this session (Section 7); unchanged from pre-batch coverage class |
| `docs/technique-census-analysis.md` prose | n/a (documentation) | `check:documentation-links` | structural | passes (Section 10) |

## 4. Compatibility and frozen-history ownership

No row in this batch is `persistence: dual-read`; NC-P08-022 is `persistence: frozen-history` (the doc rename itself, not a data-read boundary) with no `compatibility` object required per the ledger schema.

| Row ID | Legacy form | Canonical form | Mode / retireWhen | Owning boundary | Frozen artifacts unchanged |
| --- | --- | --- | --- | --- | --- |
| NC-P08-021 | `technique-census-second-order.mjs` | `analyze-technique-census.mjs` | n/a (direct rename, no persisted-data boundary) | n/a | yes |
| NC-P08-022 | `technique-census-second-order-analysis.md` | `technique-census-analysis.md` | `frozen-history` persistence | n/a | yes — the generated-report title/filenames it documents remain frozen (Section 1) |
| NC-P08-042 | `analyze-equal-work-census-pilot.mjs` | `analyze-equal-work-census.mjs` | n/a (direct rename, no persisted-data boundary) | n/a | yes |

### 4.1 High-risk rollback plan

No row in this batch is `risk: high`, and none are dual-read. Rollback is a trivial revert of the batch commit(s); no persisted data or compatibility alias depends on either spelling.

## 5. Before-change baseline

This batch is `medium` risk at most and behavior-preserving by construction (renames/comment edits only; `analyze-equal-work-census.mjs` required no content edit at all). Baselines captured:

| Command / fixture | Before result / fingerprint |
| --- | --- |
| `node scripts/technique-census-second-order.mjs reports/stress/technique-census/32240161854 --check` (pre-rename, via `git stash`) | fails with `Stale second-order generated output: .../second-order-analysis.json, .../second-order-analysis.md` |
| `node scripts/analyze-technique-census.mjs reports/stress/technique-census/32240161854 --check` (post-rename) | fails with the **identical** message/file list — confirms this batch introduced no new staleness, the condition is pre-existing |
| `node scripts/analyze-technique-census.mjs reports/stress/technique-census/32240161854 <tmp-out.json> <tmp-out.md>` (post-rename, no `--check`) | `Analyzed 76614 T1 cells across 41 techniques; wrote <tmp-out.json> and <tmp-out.md>` — real successful analysis against the real committed 41-technique/76614-cell dataset |

Behavioral parity for the doc-only row (NC-P08-022) is established by the documentation-link check and the tool-side evidence above (the doc primarily describes the tool's real behavior, which is unchanged).

## 6. Implementation log

- Renamed `scripts/technique-census-second-order.mjs` -> `scripts/analyze-technique-census.mjs` and its node-test to `scripts/analyze-technique-census-node-test.mjs`; updated the generated-markdown self-reference string ("Generated by `scripts/...`") and the node-test's own console-log label and import path; updated `package.json`'s `test:technique-census-second-order` -> `test:analyze-technique-census` (including the `test:node` aggregate string).
- Renamed `docs/technique-census-second-order-analysis.md` -> `docs/technique-census-analysis.md`; updated its title and rebuild-command example; updated every cross-reference in `docs/README.md`, `docs/tooling-catalog.md` (2 mentions), `docs/solver-correctness-hardening.md`, `docs/testing.md`, `docs/solver-scheduling-policy.md`, and `docs/solver-technique-operational-taxonomy.md`; deliberately left the `second-order-analysis.json.techniqueBudgetCurves` generated-field reference inside the renamed doc untouched (Section 1).
- Renamed `scripts/stress/analyze-equal-work-census-pilot.mjs` -> `scripts/stress/analyze-equal-work-census.mjs` (no internal content needed editing); updated `scripts/naming-cleanup-phase8-cli-smoke-node-test.mjs`'s fast-fail check target and `docs/tooling-catalog.md`'s one reference.
- Investigated and deliberately left unrenamed: the generated report's own title text, the `second-order-analysis.json`/`.md` default output filenames, `scripts/technique-operational-similarity.mjs`'s hardcoded default `--source` path, and the internal function name `renderTechniqueCensusSecondOrder` — all tied to the genuinely persisted, already-committed generated-report identity under `reports/stress/technique-census/32240161854/` (Section 1).
- Updated `docs/naming-cleanup-plan.md` notation for all three rows: converted old-name mentions to plain (non-canonical-claiming) text where they described retired identities, consistent with prior batches' notation convention (no bare-alias-existence checker issue existed for this batch, since none of its rows use a `namespace:alias`-shaped npm command name).

## 7. Targeted contract validation

| Command / test | Boundary proved | Result |
| --- | --- | --- |
| `npx tsc --noEmit -p tsconfig.json` | full TypeScript compile | pass (0 errors; this batch touches no `.ts` files) |
| `npx eslint <all touched .mjs files>` | lint over every edited script | pass, 0 findings |
| `node -e "JSON.parse(...)"` on `package.json` | edited JSON remains well-formed | pass |
| `node scripts/analyze-technique-census-node-test.mjs` | real synthetic-fixture execution of every exported library function | pass |
| `node scripts/analyze-technique-census.mjs reports/stress/technique-census/32240161854 <tmp-out.json> <tmp-out.md>` | real full analysis against the real committed 41-technique/76614-cell census dataset | pass — `Analyzed 76614 T1 cells across 41 techniques`; generated markdown's self-reference correctly reads `scripts/analyze-technique-census.mjs` |
| `node scripts/analyze-technique-census.mjs reports/stress/technique-census/32240161854 --check` (before vs. after, via `git stash`) | this batch introduces no new staleness | pass — identical pre-existing staleness message before and after (Section 5) |
| `node scripts/naming-cleanup-phase8-cli-smoke-node-test.mjs` | real fast-fail argument-validation execution of `analyze-equal-work-census.mjs` through its real bundling seam | pass |
| `npm run check:documentation-links` | every link/path in current docs resolves to a real, live target | pass, 1342 Markdown files |
| `npm run check` (dead-scripts, text-source-files, lint, all `check:validators`) | full repository check suite | pass |
| `npm run test:node` | full 53-script aggregate Node-test graph | pass, 53/53 |
| `npx vitest run --coverage` | full unit suite unaffected by this batch's comment/rename-only edits | pass, 105 files / 1325 tests |

## 8. Consumer-inward closeout audit

Same-session audit (no separate fresh agent was available in this environment, same limitation noted in batches 8A-8C).

- package commands and surfaced CLIs: `package.json` scans clean — no remaining `technique-census-second-order`/`analyze-equal-work-census-pilot` entries; `npm run naming:status` reports the expected batch-8D-complete state before this closeout.
- workers/raced execution: neither tool in this batch uses a worker/race transport.
- workflows and exact-case targets: `check-workflow-actions.mjs` passes (run as part of `check:validators`); no workflow YAML references either tool.
- generated-data readers/writers/analyzers: `scripts/technique-operational-similarity.mjs`'s default `--source` path was specifically checked and confirmed to still resolve correctly (it reads the untouched `second-order-analysis.json`, unaffected by the tool-file rename).
- current docs/reproduction commands: `check:documentation-links` passes; all 6 doc cross-references grep-verified updated.
- application/UI/editor consumer: not applicable, neither tool is UI-consumed.
- historical compatibility paths: `docs/archive/**`, `reports/**`, and the entire `reports/stress/technique-census/32240161854/` directory were grepped and confirmed untouched.

Findings: none outstanding within this batch's scope. The pre-existing `--check` staleness (Section 1) is noted for visibility but is explicitly not this batch's (or this naming-cleanup plan's) problem to fix.

## 9. Behavioral/evidence parity

| Observable | Before | After | Parity |
| --- | --- | --- | --- |
| `analyze-technique-census-node-test.mjs` (formerly `technique-census-second-order-node-test.mjs`) | same synthetic-fixture assertions, unchanged logic | pass | parity |
| Full analysis run against `reports/stress/technique-census/32240161854/` | (tool unrenamed; logic identical) | `Analyzed 76614 T1 cells across 41 techniques` | parity — matches the committed dataset's known scale |
| `--check` staleness result | `Stale second-order generated output: .../second-order-analysis.json, .../second-order-analysis.md` | identical message, same two files | parity — pre-existing condition, unchanged by this batch |
| `analyze-equal-work-census.mjs` fast-fail check | (tool unrenamed; logic identical) | same `--equal-work=.*required` error | parity |
| full unit/coverage suite | 105 files / 1325 tests passing pre-batch | 105 files / 1325 tests passing post-batch | parity |

No unexplained solved-set, report-completeness, UI, or workflow behavior change.

## 10. Residue and authority reconciliation

- Reconciliation mode: delta, against base SHA `e6a385a554ff176c90265f479559562afd9852fd` (current `main` immediately after batch 8C's merge). Sufficient because no unrelated Phase 8-14 architecture work landed on `main` between the 8C merge and this batch's implementation, and this batch's rows are self-contained tool/doc renames.
- Target-occupancy: see Section 2 — clear for every canonical target in this batch.
- Legacy-term residue search (excluding `node_modules/`, `.git/`, `reports/`, `docs/archive/`, `data/`, `.solver-tools/`, `.cache/`, `coverage/`): `technique-census-second-order`, `analyze-equal-work-census-pilot`, `test:technique-census-second-order` — all clean except the authority docs (intentional historical mapping record).
- Canonical-term search: confirmed present in every intended consumer (Section 6).
- `npm run check:documentation-links`: passes.
- Plan/ledger changes from newly discovered scope: none required a specification amendment. The deliberately-retained generated-report identity (title text, default filenames, `renderTechniqueCensusSecondOrder` function name) is a documented scope exclusion discovered *before* implementation (Section 2), not a surprise found afterward.
- Intentional retained/frozen hits: `docs/archive/**`, `reports/**`, the entire `reports/stress/technique-census/32240161854/` directory, the generated-report title/default-filename identity, and `scripts/technique-operational-similarity.mjs`'s unchanged default source path (all documented above).

No unclassified live hit remains in this batch's scope.

## 11. Pre-merge barrier

- [x] predecessor batch 8C's `batchCompletions` entry recorded the real merged PR/commit (PR #1588, `e6a385a554ff176c90265f479559562afd9852fd`) before this batch was claimed;
- [x] branch is current `main` (post-8C-merge) plus only this batch's commits;
- [x] compared branch head against current `main` — clean, no drift;
- [x] intended diff is non-empty and original (no prior duplicate PR found);
- [x] no unrelated next-batch (8E+) implementation is stacked in this PR;
- [x] targeted validation green (Section 7);
- [x] required aggregate CI: `npm run check`, `npm run test:node`, and the full Vitest suite with coverage were all run directly in this session and are green;
- [x] ledger IDs, risk, compatibility policy, and verification fields (updated in the same PR) match the evidence in this record;
- [x] all predecessor phases (1-7) are complete; batches 8A, 8B, and 8C are merged;
- [x] no specification amendment is smuggled into this PR — all edits implement the already-fixed Section 5.6 mappings, plus one explicit, documented scope exclusion (the generated-report identity), not a change to any canonical target, compatibility lifetime, risk class, or batch assignment;
- [ ] PR description links this record — pending PR creation;
- [x] no unexplained solved-set, report-completeness, UI, or workflow behavior change (Section 9);
- [x] all 3 selected rows are `done` and `activeExecution` is reset to `idle` in this same commit set before merge;
- [x] this batch's own `batchCompletions["8D"]` entry remains `pending` (no PR/merge commit exists yet) until it actually merges.

## 12. Closure and merge handoff

| Item | Value |
| --- | --- |
| PR | pending |
| Final head SHA | pending (recorded at push time) |
| Merged? | no |
| Ledger rows closed | NC-P08-021, NC-P08-022, NC-P08-042 |
| Deferred/superseded rows | none deferred; the generated-report identity exclusion is a documented in-batch scope decision, not a deferred row |
| Known structural-only surfaces | none new in this batch — every touched surface has direct real-execution coverage (Section 3) |

Batch 8E (prune-gap labelled-branch collection) must not start on this unmerged branch. The next batch's session must start from new current `main` after this PR merges, record this batch's merged PR/commit in ledger `batchCompletions["8D"]`, and only then claim 8E.
