# Solver future work

Deferred/reopen questions only. Current priority lives in [`solver-optimization-workstreams.md`](solver-optimization-workstreams.md); method in [`solver-research-operating-model.md`](solver-research-operating-model.md); opt-in dispositions in [`solver-opt-in-experiment-ledger.md`](solver-opt-in-experiment-ledger.md). Reconcile older questions against [`2026-09-05-solver-open-question-evidence-reconciliation.md`](../reports/2026-09-05-solver-open-question-evidence-reconciliation.md) and `research-status-index --compact` before reopening them.

## Backlog contract

This is not a second queue or experiment diary. Before promotion, state the current limitation, cheapest falsifying pilot, comparator/shared-work contract where relevant, success/stop gate, and legal level-blind production path. Move active gates to the owning workstream/specialist doc; chronology belongs in dated reports. Carry forward only the smallest unexplained residue.

## New-premise routing

The current class-4/class-5 first-loss program is also the controlled source of materially new premises for previously closed work. Do not reopen an old form because its topic has become interesting again: require a changed causal hypothesis or treatment. The current premise map distinguishes one deferred question whose old gate is already satisfied (controlled mechanic-composition transfer), narrow WS5 adjudication questions that are usable now, and WS4/WS6/WS0/handoff/selector premises that remain gated on recurring first-loss evidence. See [`2026-09-11-new-premise-reopen-map-001.md`](../reports/2026-09-11-new-premise-reopen-map-001.md).

## Algorithmic-frontier research funnel

The post-1,029 atlas leaves **388 current class-5 misses with no known rescuer**. Treat them as the algorithmic-frontier candidate cohort, not proof of absolute unsolvability. Class 4 (200 zero-T1 levels with historical cold rescues) is the preferred near-control.

Use: **class4/class5 contrast -> source-controlled profile/family comparison -> all-known-basin first loss -> smallest generic capability -> bounded frontier pilot -> independent/whole-family validation**. If no common distinction survives, split class 5 by failure/extinction phenotype. Heavy frameworks require a recurring bounded mechanism, not frontier membership alone. A row whose production-baseline status is missing remains `unknown`; it cannot enter a production-unsolved/frontier class by default. See [`2026-09-11-algorithmic-frontier-class4-vs5-contrast-001.md`](../reports/2026-09-11-algorithmic-frontier-class4-vs5-contrast-001.md) and [`solver-evaluation-evidence.md`](solver-evaluation-evidence.md).

The broad scorer-vocabulary discriminator is now closed negative: the corrected 671-level witness-only run found no class-5 enrichment in either exact 12-weight collisions or weight-invariant fixed alternative preferences. Class 4 was actually more collision-heavy, while fixed-preference rates were essentially flat. Do not buy weight/profile tuning or an exact-label campaign from that result; reuse the diagnostic only if independent first-loss evidence later nominates evaluator/rank loss. See [`2026-09-11-scorer-vocabulary-frontier-witness-only-001.md`](../reports/2026-09-11-scorer-vocabulary-frontier-witness-only-001.md) and the broader [`2026-09-11-shared-substrate-and-scoring-vocabulary-audit-001.md`](../reports/2026-09-11-shared-substrate-and-scoring-vocabulary-audit-001.md). Generic move-legality differential fuzzing remains covered by the independent oracle/solver/domain harness; extend it only for a concrete unsupported semantic seam.

## Structural technique-response extension ladder

Extend existing structural/census insight by evidence quality, not feature count: **temporal persistence -> difficulty-stratified relative advantage -> existing-family controlled flips -> source-controlled solution-space mediator -> bounded operational first divergence**. Stop when a compact distinction fails. Richer generic topology remains closed unless a mechanism nominates a specific descriptor. Stages 1-2 are DONE: 2/8 frozen pairs (`beam:objectiveFirst@5000`/`beam:intersectionHarvest@5000`, each plain vs. mechanic-buckets) clear both filters; the rest stay descriptive. Stage 3 is **blocked on a data-shape gap, not a null**: the family dataset records only the whole-ladder winning config per variant, not per-technique outcomes, so it can't test these pairs' flip directly. Reopen only after a bounded isolated-technique resolve of the two exact configs on one existing family parent per pair (no new variants). See [`extension audit`](../reports/2026-09-11-structural-technique-response-extension-audit-001.md), [`temporal+difficulty confirmation`](../reports/2026-09-11-structural-technique-response-temporal-difficulty-confirmation-001.md), [`family query`](../reports/2026-09-11-structural-technique-response-family-query-001.md).

## Deferred questions

| Question | Reopen condition / boundary |
|---|---|
| **Variant-family capability robustness** | Temporal/budget-edge fragility and multiplicity→production-success are answered; doubletons are often same-family and lack a strong coarse structural signature. For a structural-technique claim, use the existing family dataset only after temporal/difficulty screening nominates a pair, keep whole parents independent, and seek controlled response flips. Do not rerun answered joins or generate variants first. See the multiplicity/doubleton reports indexed by `research-status-index`. |
| **Four-space triangulation** across input geometry, solution structure, technique response, and live trajectories | Reopen per nominated contrast, not as a broad campaign. Require a temporally credible, difficulty-robust response distinction, then family and source-controlled profile evidence before trajectories. |
| **Mechanic-composition transfer / generator expansion** | **Bounded premise now earned; broad expansion remains closed.** The promoted must-cross-forced-neighbour x visited-portal-terminal obligation prune supplies the concrete within-corpus intervention that this gate required. A small controlled family/generator pilot may vary that coupled-obligation semantic boundary and test predicted response/first-loss flips, keeping whole parents independent. Prefer existing transforms; add portal-aware generation only where needed to isolate the contrast. See the new-premise map. |
| **Richer static graph/placement selectors** | Tested 18-feature bundle added no material held-out value. Reopen only for one mechanism-specific descriptor derived from recurring solution/family/trace/first-loss evidence; do not resume generic feature accretion. Candidate descriptor classes such as residual-interface capacity or coupled-obligation slack remain trigger-gated, not preapproved. |
| **Stability-aware portfolios** | Reopen only if WS1 basin-complementarity work finds a compact held-out decision signal beyond current work/coverage evidence. Basin diversity alone is not a production selector. |
| **Latent response dimensions / biclusters** | Reopen only if simpler pair/cohort analysis leaves stable unexplained structure after temporal and generic-difficulty controls. |
| **Repair restart allocation in the near-miss band** | Keep the unchanged 150M restart/continuation form closed. Preserve the independently replicated 64M fully-spent bestBadness<=6 restart signal; reopen only on a fresh legal population with the same texture or a changed allocation contract. |
| **Beam continuation / cross-policy handoff** | Same-policy portfolio residual tranches are closed null; simple beam→DFS handoff and repeated/staged switching are closed in tested forms. Reopen only for a materially new premise: either a distinct future beam policy with credible complementarity or an earned first-loss/state-selection mechanism that demonstrably identifies partial states retaining live futures. Preserve continuation ownership and pooled-state-detachment invariants. |
| **Generator/editor-envelope technique niches** | Corpus1-vs-corpus2 ease, repertoire, regime composition, and structural risk are characterized; richer topology did not improve prediction. Reopen only with a joinable finer envelope or an envelope-specific mechanism claim. |
| **Minimal technique-niche counterexamples** | Use reducer + exact/reference validation when a temporally/difficulty-robust niche needs causal isolation. Exact paths should adjudicate known-live loss, not justify broad CP-SAT acquisition. |
| **Typed producer → consumer search artifacts** | Require a demonstrated consumer limitation, novel timely information, bounded production/storage/replay cost, independent control, and matched-work benefit. Do not build a general blackboard. |
| **Queryable analytical layer** | Reopen only if recurring joins still require bespoke scripts after existing join helpers are extended. Generated views must be rebuildable and cannot become production-policy truth. |
| **Class-1 compact beam-menu exposure** | Naive append/insert under `strictTotalWorkBudget=true` is closed: 17/25 of the current class-1 population resolves into the two very-high-intersection `ATTEMPT_POLICY` rules where `STRATEGY_HIGHINT_STANDARD_INTERSECTION_HARVEST_BEAM_EXPOSURE`/`..._RESERVE_PRESERVING_EXPOSURE` already proved any addition dilutes an existing protected-suffix winner's usable work, regardless of placement. Reopen only with a non-strict/additive-tier work envelope, or restricted to the 8/25 subpopulation outside those two rules with reserve-window headroom checked first. See [`reconciliation`](../reports/2026-09-11-class1-compact-beam-menu-budget-dilution-reconciliation-001.md). |

The census cross-evidence plan is a standing evidence map, not an active sequence: Gate 0 is complete, the bounded Gate-1 pilot was inconclusive, and later routes reopen only for a current decision-bearing question. Source-stratified hint/path mining and basin analysis already promoted to WS1 belong in the live workstream authority, not here.

## Demoted forms

Do not reopen unchanged without materially new evidence:

- whole-ladder retries, global seed fan-out, nearby-threshold widening, or broad extra repair work that mainly buys dose; a behavior-identical forced retry is not a distinct treatment and must not receive a second funded pass;
- hand-authored scorer proliferation, universal beam-width increases, or large novelty/MAP-Elites/DPP machinery before a simple descriptor-aware treatment earns it;
- broad ZDD/DD/frontier, representative-set, automaton, CEGAR/interpolation, RCSP/label-setting, CDCL/LCG, or backdoor frameworks before a bounded residual mechanism demonstrates value;
- exact/approximate caching without new sound recurrence/sufficiency evidence;
- rotate/mirror production retries or broad symmetry canonicalization instead of causal first-divergence diagnosis;
- hazard/bandit/ML scheduling before simpler fixed-work evidence demonstrates actionable headroom;
- bulk variants, full-corpus A/Bs for causally falsified ideas, retained closed experiment code, proxy optimization after solve/work/correctness failure, or framework-building before the smallest value-of-information pilot succeeds.

Keep this file short: questions, boundaries, reopen conditions only.