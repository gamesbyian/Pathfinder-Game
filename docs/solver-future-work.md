# Solver future work

Deferred/reopen questions only. Current priority: [`solver-optimization-workstreams.md`](solver-optimization-workstreams.md); method: [`solver-research-operating-model.md`](solver-research-operating-model.md); capability-memory interpretation: [`solver-capability-memory.md`](solver-capability-memory.md); opt-in dispositions: [`solver-opt-in-experiment-ledger.md`](solver-opt-in-experiment-ledger.md). Reconcile older questions against [`evidence reconciliation`](../reports/2026-09-05-solver-open-question-evidence-reconciliation.md) and `research-status-index --compact` first.

## Backlog

This is not a second queue or experiment diary. Before promotion, state the current limitation, cheapest falsifying pilot, comparator/shared-work contract where relevant, success/stop gate, and legal level-blind production path. Move active gates to the owning workstream/specialist doc. Carry forward only the smallest unexplained residue.

## New-premise routing

The current class-4/class-5 first-loss program is also the controlled source of materially new premises for previously closed work. Do not reopen an old form because its topic has become interesting again: require a changed causal hypothesis or treatment. The current premise map distinguishes one deferred question whose old gate is already satisfied (mechanic-composition transfer), narrow WS5 questions usable now, and WS4/WS6/WS0/handoff/selector premises still gated on recurring first-loss evidence. See [`new-premise map`](../reports/2026-09-11-new-premise-reopen-map-001.md).

Capability memory is an additional **premise source, not a reopen exemption**. A closed treatment's complementary gain/loss signature, a displaced winner from an accepted production change, or an old solver regime may nominate a new generic mechanism when it recurs or aligns with current first-loss/family/trace evidence. Historical same-level outcomes remain offline nomination evidence until reconciled to current code/protocol, and the descendant must still satisfy the owning reopen condition plus ordinary level-blind evidence gates. See [`solver-capability-memory.md`](solver-capability-memory.md).

## Algorithmic-frontier research funnel

The provenance-audited post-1,029 atlas leaves **445 current class-5 misses with no known admissible/T1 candidate**. Treat them as the algorithmic-frontier candidate cohort, not proof of absolute unsolvability. Class 4 (**143** zero-T1 levels with an explicit historical production-context candidate after provenance reconciliation) is the preferred near-control. The older 388/200 split is pre-audit historical sizing and must not be reused as current class membership.

Use: **class4/class5 contrast -> source-controlled profile/family comparison -> all-known-basin first loss -> smallest generic capability -> bounded frontier pilot -> independent/whole-family validation**. If no common distinction survives, split class 5 by failure/extinction phenotype. Heavy frameworks require a recurring bounded mechanism, not frontier membership alone. A row whose production-baseline status is missing remains `unknown`; it cannot enter a production-unsolved/frontier class by default. Historical capability-memory intersections may help nominate contrasts within this funnel, but they do not convert a class-5 row into known current capability without current comparable evidence. See [`class4-vs5 contrast`](../reports/2026-09-11-algorithmic-frontier-class4-vs5-contrast-001.md), [`post-1,029 residual atlas`](../reports/2026-09-11-post-1029-residual-atlas-001.md), [`provenance audit`](../reports/2026-09-11-hint-provenance-evidence-relevance-audit-001.md), and [`solver-evaluation-evidence.md`](solver-evaluation-evidence.md).

The broad scorer-vocabulary discriminator is closed negative: the corrected 671-level witness-only run found no class-5 enrichment in exact 12-weight collisions or weight-invariant fixed preferences (class 4 was actually more collision-heavy). Do not buy weight/profile tuning from that result; reuse only if independent first-loss evidence later nominates evaluator/rank loss. See [`scorer-vocabulary`](../reports/2026-09-11-scorer-vocabulary-frontier-witness-only-001.md). Generic move-legality fuzzing remains covered by the independent oracle/solver/domain harness; extend only for a concrete unsupported semantic seam.

## Structural technique-response extension ladder

Extend existing structural/census insight by evidence quality, not feature count: **temporal persistence -> difficulty-stratified relative advantage -> existing-family controlled flips -> source-controlled solution-space mediator -> bounded operational first divergence**. Stop when a compact distinction fails. Stages 1-2 are DONE: 2/8 frozen pairs (`beam:objectiveFirst@5000`/`beam:intersectionHarvest@5000`, each plain vs. mechanic-buckets) clear both filters; the rest stay descriptive. Stage 3's data-shape gap is now resolved and stage 3 itself succeeds for both pairs: a bounded isolated-technique resolve (both exact configs, matched 20M-work budget, one existing family parent per pair — `R02687`/objectiveFirst, `R02094`/intersectionHarvest — no new variant generation) found real per-sibling response flips within 4/5 existing family transform modes for each pair, with the two parents flipping in **opposite** net directions (see [`stage-3 isolated resolve report`](../reports/2026-09-11-ws1-stage3-isolated-technique-resolve-001.md)). Two cheap zero-compute mediator candidates (static object counts; portal-use timing) both fail to separate winners — the real mediator, if compact, needs must-cross visitation order or a basin/portal-set descriptor, not tried yet. Next earned stage: that source-controlled solution-space mediation, or bounded operational first divergence, for these two pairs specifically — not a third pair or a bulk campaign. See [`extension audit`](../reports/2026-09-11-structural-technique-response-extension-audit-001.md), [`family query`](../reports/2026-09-11-structural-technique-response-family-query-001.md), [`stage-3 resolve`](../reports/2026-09-11-ws1-stage3-isolated-technique-resolve-001.md).

## Deferred questions

| Question | Reopen condition / boundary |
|---|---|
| **Variant-family capability robustness** | Temporal/budget-edge fragility and multiplicity→production-success are answered; doubletons are often same-family and lack a strong coarse structural signature. For a structural-technique claim, use the family dataset only after temporal/difficulty screening nominates a pair, keep whole parents independent, seek controlled response flips. Do not rerun answered joins or generate variants first. See the multiplicity/doubleton reports via `research-status-index`. |
| **Four-space triangulation** (geometry, solution structure, technique response, trajectories) | Reopen per nominated contrast, not as a broad campaign. Require a temporally credible, difficulty-robust response distinction, then family/profile evidence before trajectories. |
| **Mechanic-composition transfer / generator expansion** | **Bounded premise now earned; broad expansion remains closed.** The promoted must-cross-forced-neighbour x visited-portal-terminal obligation prune supplies the concrete within-corpus intervention this gate required. A small controlled family/generator pilot may vary that coupled-obligation boundary and test predicted response/first-loss flips, keeping whole parents independent. Prefer existing transforms. See the new-premise map. |
| **Richer static graph/placement selectors** | Tested 18-feature bundle added no material held-out value. Reopen only for one mechanism-specific descriptor derived from recurring solution/family/trace/first-loss evidence; do not resume generic feature accretion. Descriptor classes like residual-interface capacity or coupled-obligation slack remain trigger-gated. |
| **Stability-aware portfolios** | Reopen only if WS1 basin-complementarity work finds a compact held-out decision signal beyond current work/coverage evidence; basin diversity alone is not a selector. Capability-memory union/overlap may nominate the contrast, but is not itself a routing signal. |
| **Latent response dimensions / biclusters** | Reopen only if simpler pair/cohort analysis leaves stable unexplained structure after temporal/difficulty controls. |
| **Repair restart allocation in the near-miss band** | Keep the unchanged 150M restart/continuation form closed. Preserve the replicated 64M fully-spent bestBadness<=6 restart signal; reopen only on a fresh legal population with the same texture or a changed allocation contract. |
| **Beam continuation / cross-policy handoff** | Same-policy portfolio residual tranches are closed null; simple beam→DFS handoff and repeated/staged switching are closed in tested forms. Reopen only for a distinct future beam policy with credible complementarity, or an earned first-loss/state-selection mechanism identifying partial states with live futures. Preserve continuation ownership/pooled-state-detachment invariants. |
| **Generator/editor-envelope technique niches** | Corpus1-vs-corpus2 ease, repertoire, regime composition, structural risk are characterized; richer topology did not improve prediction. Reopen only with a joinable finer envelope or an envelope-specific mechanism claim. |
| **Minimal technique-niche counterexamples** | Use reducer + exact/reference validation when a temporally/difficulty-robust niche needs causal isolation. Exact paths adjudicate known-live loss, not broad CP-SAT acquisition. |
| **Typed producer → consumer search artifacts** | Require a demonstrated consumer limitation, novel timely information, bounded production/storage/replay cost, independent control, matched-work benefit. Do not build a general blackboard. |
| **Queryable analytical layer** | Reopen only if recurring joins still require bespoke scripts after join helpers are extended. Generated views must be rebuildable, never production-policy truth. The capability-memory view is intentionally a narrow rebuildable interface, not a general analytical warehouse. |
| **Class-1 compact beam-menu exposure** | Naive append/insert under `strictTotalWorkBudget=true` is closed: 17/25 of class-1 resolves into the two very-high-intersection `ATTEMPT_POLICY` rules where `STRATEGY_HIGHINT_STANDARD_INTERSECTION_HARVEST_BEAM_EXPOSURE`/`..._RESERVE_PRESERVING_EXPOSURE` already proved any addition dilutes an existing protected-suffix winner's work. Reopen only with a non-strict/additive-tier envelope, or the 8/25 subpopulation outside those rules with reserve headroom checked first. See [`reconciliation`](../reports/2026-09-11-class1-compact-beam-menu-budget-dilution-reconciliation-001.md). |

The census cross-evidence plan is a standing evidence map, not an active sequence: Gate 0 is complete, the bounded Gate-1 pilot was inconclusive, later routes reopen only for a current decision-bearing question. Source-stratified hint/path mining and basin analysis already promoted to WS1 belong in the live workstream authority.

## Demoted forms

Do not reopen unchanged without materially new evidence:

- whole-ladder retries, global seed fan-out, nearby-threshold widening, or broad extra repair work that mainly buys dose; a behavior-identical forced retry is not a distinct treatment;
- hand-authored scorer proliferation, universal beam-width increases, or large novelty/MAP-Elites/DPP machinery before a descriptor-aware treatment earns it;
- broad ZDD/DD/frontier, representative-set, automaton, CEGAR/interpolation, RCSP/label-setting, CDCL/LCG, or backdoor frameworks before a bounded residual mechanism demonstrates value;
- exact/approximate caching without new sound recurrence/sufficiency evidence;
- rotate/mirror production retries or broad symmetry canonicalization instead of first-divergence diagnosis;
- hazard/bandit/ML scheduling before simpler fixed-work evidence shows actionable headroom;
- bulk variants, full-corpus A/Bs for falsified ideas, retained closed experiment code, proxy optimization after solve/work/correctness failure, or framework-building before the smallest info-value pilot succeeds.

Keep this file short: questions, boundaries, reopen conditions.
