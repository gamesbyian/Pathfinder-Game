# Stratified-sample level-blind capability A/B for a routing-regime-gated ATTEMPT_POLICY routing
# change (modules/solver/attempts.ts) -- the general form of solver-repair-probe-adaptive-sample-
# ab.yml's eligibility-sample pattern, generalized from one hardcoded mechanic predicate to any set
# of classifyRoutingRegime() routing regimes via scripts/stress/select-routing-regime-sample.mjs.
#
# Deliberately NOT a full-corpus refresh, and NOT solver-stress-refresh.yml's canonical full-1700
# capability workflow: an ATTEMPT_POLICY rule change can only ever affect a level's outcome if the
# level's classified routing regime matches one of the rules the change touches (attempts.ts routes purely
# on classifyRoutingRegime(level)/LevelFeatures). A uniform random sample of the full corpus wastes most
# of its compute re-confirming zero-effect on every other routing regime. This draws a deterministic
# seeded subsample from the named routing regime(s) specifically, plus a small control sample from every
# other routing regime, to empirically catch anything outside the intended scope. Same seed -> same
# sample, always.
#
# CORPUS INVARIANT: every arm also runs all of Corpus 1 and all published levels; the targeted
# sample applies only to expensive Corpus 2. scripts/plan-ab-corpus-shards.mjs owns this shared policy.
#
# CAPABILITY INVARIANT (same as solver-stress-refresh.yml): uses scripts/level-blind-capability-
# sweep.mjs, never the history-aware research sweep. Never persists hints, never touches
# logs/stress-corpus2-baseline.json / data/level-heatmaps.json. Artifact-only: never commits to main.
#
# Usage: dispatch twice with identical inputs (including routing_regimes and seed) except enable_flags/
# disable_flags/ref for whatever code change you're A/B testing -- e.g. dispatch control at the
# pre-change commit and treatment at the post-change commit, both with identical routing_regimes/seed/
# node_budget -- then compare each run's combined solved count/gains/losses (per-id, from each run's
# reports/stress/routing-regime-sample-ab.json artifact).
name: Solver routing-regime-gated routing sample A/B
run-name: "Routing regime sample · ${{ inputs.routing_regimes || inputs.archetypes || 'missing' }} · flags=${{ inputs.enable_flags || 'control' }}"

on:
  workflow_dispatch:
    inputs:
      routing_regimes:
        description: 'Comma-separated classifyRoutingRegime() values the code change under test can affect, e.g. intersection-heavy,must-cross-heavy (see modules/solver/routing-regime.ts for the full vocabulary: sparse-low-intersection, multi-portal, must-cross-heavy, intersection-heavy, general).'
        required: false
        default: ''
      archetypes:
        description: 'Deprecated compatibility alias for routing_regimes. Legacy values are normalized by the selector; do not set both inputs to different values.'
        required: false
        default: ''
      eligible_sample:
        description: 'How many of the eligible (routing regime in the list above) population to sample.'
        required: false
        default: '250'
      control_sample:
        description: 'How many ineligible (every other routing regime) levels to sample as a control.'
        required: false
        default: '50'
      seed:
        description: 'Deterministic sample seed. MUST be identical across both arms of a matched A/B -- pick something stable per question (e.g. a short slug for what you are testing) so re-dispatches of the same question stay comparable.'
        required: false
        default: 'routing-regime-sample'
      node_budget:
        description: 'Corpus-2 base node allocation per level. Matches solver-stress-refresh.yml''s canonical corpus2_node_budget default; additive tiers can exceed it unless strict_total_work_budget=true.'
        required: false
        default: '50000000'
      strict_total_work_budget:
        description: 'If true, enforce the derived work budget as a true whole-solve ceiling. Default false preserves legacy additive-pass semantics.'
        required: false
        default: 'false'
      budget_ms:
        description: 'Per-level wall-safety deadline. Default 24h is intentionally non-binding; allocation is governed by node/work inputs, with additive tiers unless strict_total_work_budget=true.'
        required: false
        default: '86400000'
      workers:
        description: 'Cross-level worker processes per shard. Default 4 matches current standard public ubuntu-latest runner vCPU count.'
        required: false
        default: '4'
      shard_count:
        description: 'Number of even shards to split the sample into. Default 60 queues finer work behind 20 lanes so fast lanes keep pulling work instead of idling behind a coarse-shard straggler.'
        required: false
        default: '60'
      max_parallel:
        description: 'Shards running concurrently. Keep below shard_count for queue-based tail balancing; lower further if a matched A/B pair must share the account job budget.'
        required: false
        default: '20'
      enable_flags:
        description: 'Comma-separated ablation flags to turn ON for this arm.'
        required: false
        default: ''
      disable_flags:
        description: 'Comma-separated ablation flags to turn OFF for this arm.'
        required: false
        default: ''
      repair_late_probe_node_budget:
        description: 'Optional STRATEGY_REPAIR_LATE_PROBE node cap override (production default 2000000, stage-budget.ts REPAIR_LATE_PROBE_NODE_BUDGET); blank for control.'
        required: false
        default: ''
      deterministic:
        description: 'true forces a 24h non-binding deadline so this run is reproducible (bit-identical nodes/work). Matched A/B arms should both use true.'
        required: false
        default: 'true'

concurrency:
  group: solver-routing-regime-sample-ab-${{ inputs.routing_regimes || inputs.archetypes || 'missing' }}-${{ inputs.seed }}
  cancel-in-progress: false

jobs:
  plan:
    name: Select stratified sample and plan shards
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    outputs:
      shards: ${{ steps.plan.outputs.shards }}
      shard_count: ${{ steps.plan.outputs.shard_count }}
      total_levels: ${{ steps.plan.outputs.total_levels }}
    steps:
      - name: Check out the dispatched commit exactly
        uses: actions/checkout@v7
        with:
          ref: ${{ github.sha }}
          sparse-checkout: |
            /*
            !/logs/
            !/reports/
          sparse-checkout-cone-mode: false

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: '20'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Select the deterministic stratified sample
        shell: bash
        run: |
          mkdir -p logs/solver-routing-regime-sample
          routing_regimes="${{ inputs.routing_regimes }}"
          legacy_archetypes="${{ inputs.archetypes }}"
          if [ -z "$routing_regimes" ] && [ -z "$legacy_archetypes" ]; then
            echo "routing_regimes is required (deprecated alias: archetypes)" >&2
            exit 2
          fi
          if [ -n "$routing_regimes" ] && [ -n "$legacy_archetypes" ] && [ "$routing_regimes" != "$legacy_archetypes" ]; then
            echo "conflicting routing_regimes/archetypes inputs" >&2
            exit 2
          fi
          resolved_regimes="${routing_regimes:-$legacy_archetypes}"
          node scripts/run-bundled.mjs scripts/stress/select-routing-regime-sample.mjs -- \
            --corpus=data/stress/stress-levels-random.json \
            --routing-regimes="$resolved_regimes" \
            --eligible-sample="${{ inputs.eligible_sample }}" \
            --control-sample="${{ inputs.control_sample }}" \
            --seed="${{ inputs.seed }}" \
            --out=logs/solver-routing-regime-sample/sample.txt

      - name: Split into even shards
        id: plan
        shell: bash
        run: |
          node scripts/plan-ab-corpus-shards.mjs \
            --corpus2-sample="logs/solver-routing-regime-sample/sample.txt" \
            --corpus2-shards="${{ inputs.shard_count }}"

      - name: Upload the sample + shard plan for traceability
        uses: actions/upload-artifact@v7
        with:
          name: routing-regime-sample-plan
          path: logs/solver-routing-regime-sample/
          retention-days: 90

  solve-shards:
    name: Sample shard ${{ matrix.shard.idx }}/${{ needs.plan.outputs.shard_count }}
    needs: [plan]
    runs-on: ubuntu-latest
    timeout-minutes: 180
    permissions:
      contents: read
    strategy:
      fail-fast: false
      max-parallel: ${{ fromJson(inputs.max_parallel) }}
      matrix: ${{ fromJson(needs.plan.outputs.shards) }}
    steps:
      - name: Check out the dispatched commit exactly
        uses: actions/checkout@v7
        with:
          ref: ${{ github.sha }}
          sparse-checkout: |
            /*
            !/logs/
            !/reports/
          sparse-checkout-cone-mode: false

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: '20'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run level-blind capability sweep on this shard
        id: sweep
        continue-on-error: true
        shell: bash
        env:
          DEADLINE: ${{ inputs.deterministic == 'true' && '86400000' || inputs.budget_ms }}
        run: |
          set +e
          mkdir -p logs/solver-routing-regime-sample
          extra=()
          [ -n "${{ inputs.enable_flags }}" ] && extra+=("--enable-flags=${{ inputs.enable_flags }}")
          [ -n "${{ inputs.disable_flags }}" ] && extra+=("--disable-flags=${{ inputs.disable_flags }}")
          [ -n "${{ inputs.repair_late_probe_node_budget }}" ] && extra+=("--repair-late-probe-node-budget=${{ inputs.repair_late_probe_node_budget }}")
          work=$(( ${{ inputs.node_budget }} * 134 / 100 ))
          strict=()
          [ "${{ inputs.strict_total_work_budget }}" = "true" ] && strict+=("--strict-total-work-budget")
          timeout -k 30s --preserve-status 170m node scripts/run-bundled.mjs scripts/level-blind-capability-sweep.mjs -- \
            --corpus="${{ matrix.shard.corpus }}" \
            --levels="${{ matrix.shard.levels }}" \
            --budget-ms="${DEADLINE}" \
            --node-budget="${{ inputs.node_budget }}" \
            --work-budget="${work}" \
            "${strict[@]}" \
            --workers="${{ inputs.workers }}" \
            "${extra[@]}" \
            --out="logs/solver-routing-regime-sample/batch-${{ matrix.shard.idx }}.json" \
            --summary-out="logs/solver-routing-regime-sample/batch-${{ matrix.shard.idx }}-summary.md"
          status=$?
          echo "exit_code=${status}" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Upload shard artifact
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: routing-regime-sample-shard-${{ matrix.shard.idx }}
          path: |
            logs/solver-routing-regime-sample/batch-${{ matrix.shard.idx }}.json
            logs/solver-routing-regime-sample/batch-${{ matrix.shard.idx }}-summary.md
          if-no-files-found: error
          retention-days: 90

      - name: Fail on unexpected sweep error
        if: >-
          always() &&
          (steps.sweep.outputs.exit_code != '0' && steps.sweep.outputs.exit_code != '124' && steps.sweep.outputs.exit_code != '143')
        run: exit 1

  combine:
    name: Combine sample results
    needs: [plan, solve-shards]
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
    steps:
      - name: Check out the dispatched commit exactly
        uses: actions/checkout@v7
        with:
          ref: ${{ github.sha }}
          sparse-checkout: |
            /*
            !/logs/
            !/reports/
          sparse-checkout-cone-mode: false

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: '20'

      - name: Download shard artifacts
        uses: actions/download-artifact@v8
        with:
          pattern: 'routing-regime-sample-shard-*'
          path: artifact-staging/downloaded
          merge-multiple: true

      - name: Combine into one report (subset-shaped -- NEVER the full-corpus baseline)
        shell: bash
        run: |
          mkdir -p logs/solver-routing-regime-sample reports/stress
          cp -a artifact-staging/downloaded/. logs/solver-routing-regime-sample/
          FILES=$(ls logs/solver-routing-regime-sample/batch-*.json 2>/dev/null | paste -sd, -)
          [ -n "$FILES" ] && node scripts/portfolio-sweep-reports-to-benchmark.mjs --allow-mixed-corpora --in="${FILES}" --out=reports/stress/routing-regime-sample-ab.json

      - name: Verify complete coverage
        id: coverage
        shell: bash
        run: |
          node -e "
          const fs = require('node:fs');
          const expectedTotal = ${{ needs.plan.outputs.total_levels }};
          const report = JSON.parse(fs.readFileSync('reports/stress/routing-regime-sample-ab.json', 'utf8'));
          const rows = report.levels ?? [];
          if (rows.length !== expectedTotal) { console.error(\`incomplete: \${rows.length}/\${expectedTotal}\`); process.exit(1); }
          console.log(\`complete: \${rows.length}/\${expectedTotal}\`);
          "

      - name: Print solved-count summary
        shell: bash
        run: |
          node -e "
          const fs = require('node:fs');
          const r = JSON.parse(fs.readFileSync('reports/stress/routing-regime-sample-ab.json', 'utf8'));
          const rows = r.levels ?? [];
          const solved = rows.filter(x => x.ok).length;
          const nodes = rows.reduce((n,x) => n + (x.nodesExpanded || 0), 0);
          const work = rows.reduce((n,x) => n + (x.workSpent || 0), 0);
          console.log('routing_regimes=${{ inputs.routing_regimes || inputs.archetypes }} enable_flags=${{ inputs.enable_flags }} disable_flags=${{ inputs.disable_flags }} ref=${{ github.sha }}');
          console.log(\`SOLVED sample: \${solved}/\${rows.length} nodes=\${nodes} work=\${work}\`);
          console.log('Solved ids: ' + rows.filter(x => x.ok).map(x => x.id ?? String(x.level)).sort().join(','));
          console.log('Unsolved ids: ' + rows.filter(x => !x.ok).map(x => x.id ?? String(x.level)).sort().join(','));
          "

      - name: Upload combined results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: routing-regime-sample-ab-combined
          path: |
            reports/stress/routing-regime-sample-ab.json
            logs/solver-routing-regime-sample/
          if-no-files-found: error
          retention-days: 90

      - name: Publish standard solver sweep result
        if: always()
        shell: bash
        run: |
          node scripts/publish-solver-sweep-result.mjs \
            --primary="reports/stress/routing-regime-sample-ab.json" \
            --shards-expected="${{ needs.plan.outputs.shard_count }}" \
            --shards-observed="$(find logs/solver-routing-regime-sample -maxdepth 1 -name 'batch-*.json' | wc -l | tr -d ' ')" \
            --source-artifact="routing-regime-sample-ab-combined"

      - name: Upload standard solver sweep result
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: solver-sweep-result
          path: logs/solver-sweep-result/
          if-no-files-found: error
          retention-days: 90
